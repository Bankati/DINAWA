# Progress Tracker — WARAH

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Audit de cohérence code ↔ tracker (2026-08-06)** — ce fichier n'avait pas été mis à jour depuis le 2026-07-28 alors que du code backend réel a changé depuis (commit `9064988`, 2026-08-04, auteur différent de cette session). Écarts trouvés et corrigés ci-dessous :

- **Unité 07 (vérification CNI) intégralement retirée le 2026-08-04**, alors que ce fichier la documentait encore comme terminée et testée. Le module `src/modules/identity/` (service, controller, listener OCR), le modèle Prisma `IdentityVerification`, l'énum `IdVerificationStatus`, le helper `assertIdentityVerified()` et ses tests ont tous été supprimés. Les dépendances `tesseract.js`/`mrz` ont été retirées de `package.json`. Voir la case décochée plus bas et la section Decisions mise à jour.
- **Conséquence sur l'unité 08** : `PropertiesService.create()` n'appelle plus `assertIdentityVerified()` — la création de bien n'est plus bloquée par un statut de vérification d'identité, qui n'existe plus du tout. `POST /api/auth/signup/owner`/`signup/manager` n'acceptent plus de fichiers CNI (DTOs JSON purs, plus de `FileFieldsInterceptor`).
- **`AdminModule` existe maintenant** (`src/modules/admin/`, importé dans `app.module.ts`) avec `GET /api/admin/users` et `GET /api/admin/users/:id` — un début non documenté de l'unité 37 (Phase 10), construit pour les pages frontend `/admin/comptes`. Aucun test (`admin.service.spec.ts`/`admin.controller.spec.ts` absents), pas de DTO de query (`@Query()` bruts), 4 warnings ESLint (retours de fonction non typés). Très loin du périmètre complet de l'unité 37 (pas de suspend/reactivate/transactions/identity-verifications — ce dernier n'a plus de sens sans le module identity).
- **`GET /api/payments` et `GET /api/payments/:id/receipt.pdf` acceptent désormais le rôle `TENANT`** (`payments.controller.ts`) — nécessaire pour les pages frontend locataire (historique, déclaration), cohérent avec le build-plan mais jamais noté explicitement dans ce fichier au moment du changement.
- **Suite de tests réelle au 2026-08-06** : `npx jest` → 25 suites, **257 tests**, tous passants (le fichier disait encore 267 après l'unité 28 — écart dû aux ~10 tests du module identity supprimés). `npx tsc --noEmit` propre. `npx eslint src/` → **1 erreur réelle** (`account.controller.spec.ts:14`, `@typescript-eslint/no-unnecessary-type-assertion` sur un cast de mock déjà accompagné d'un `eslint-disable` devenu inexact) + 4 warnings sur le nouveau module admin — non corrigé dans cette passe (audit seul, pas de code applicatif modifié).
- **Décision produit à reconfirmer avec le développeur** : la CNI était présentée comme une garantie de confiance structurante (bloque la création de bien tant que non vérifiée). Son retrait silencieux change un invariant de sécurité produit sans qu'aucune décision explicite n'apparaisse dans ce fichier — seul le message du commit `9064988` ("simplifie l'auth") en donne la raison. À confirmer : décision assumée définitivement, ou vérification CNI à reconstruire plus tard (voir Open Questions) ?

**Phase :** Phases 1 à 5 **terminées à l'exception de l'unité 07, retirée** (unités 01-06, 08-16, 19-25, 28 ; unité 07 vérification CNI construite puis intégralement retirée le 2026-08-04 ; unités 17-18 Cashpay explicitement reportées ; unité 23 partielle — export PDF/XLSX reporté). **Phase 8 (Gestionnaires immobiliers) intégralement terminée** le 2026-08-07/08 (unités 31 Mandats, 32 Tableau de bord gestionnaire, 33 Rapports mensuels, 34 Profil public et avis) — voir Progress ci-dessous. **Phase 9 (Abonnements) terminée pour son unique unité constructible** le 2026-08-08/09 (unité 35 Forfaits et quotas ; unité 36 Prélèvement mensuel reportée — dépendance Cashpay). Phases 6 et 11 non commencées côté backend (Phase 6 "tableaux de bord" déjà couverte côté frontend par composition client des routes existantes — voir `dashboard.service.ts`). Phase 10 démarrée de façon minimale et non planifiée : `AdminModule` partiel (unité 37 très incomplète, mais dispose désormais aussi d'un endpoint de modération d'avis posé par l'unité 34), unités 38-39 non commencées.

**Aperçus visuels des 11 templates email** générés en PNG (Chrome headless) et conservés dans `contexte/email-previews/` — utile pour validation design avec le client.

**Last completed :** Unité 06 Préférences de notifications et infrastructure push web (2026-07-01) — `WebPushService` (`src/modules/push/web-push.service.ts` : `subscribe`, `unsubscribe`, `sendToUser` avec timeout 10s + retry `p-retry` 3 tentatives sur les échecs transitoires, suppression immédiate sans retry sur 410/404 via `AbortError`), endpoints `GET /api/push/vapid-public-key`, `POST /api/push/subscribe`, `POST /api/push/unsubscribe`. `NotifyService.notifyUser()` (`src/modules/notify/notify.service.ts`) — point d'entrée unique, bascule push/email selon `notificationConsent` + abonnements actifs, pièce jointe force l'email. Vocabulaire d'événements identique aux 9 templates email non-auth de l'Unité 04 (`NotificationEvent` = `EmailTemplate` moins les 2 templates d'auth). Testé de bout en bout avec une vraie notification reçue dans un navigateur réel (page jetable supprimée après validation).

**Revue post-implémentation (`/review`)** a trouvé et corrigé 4 problèmes avant clôture : échec silencieux de `notifyUser()` quand l'utilisateur n'a ni email ni push actif (loggue maintenant un warning), bug multi-appareils dans `unsubscribe()` (repassait `notificationConsent` à `DECLINED` même si d'autres abonnements restaient actifs — corrigé via transaction avec vérification du compte restant), absence de timeout/retry sur l'envoi push (ajoutés, alignés sur `EmailService`), `findMany` sans `take` explicite (ajouté), et `@Public()` non justifié retiré de `GET /vapid-public-key`.

**Décision de marque :** le nom du produit passe de DINAWA à **WARAH** (« La gestion locative intelligente ») — logo fourni par le client (2026-07-01). Renommage complet appliqué à tout le dépôt le même jour : contenu visible (templates email, `RESEND_FROM_NAME`), noms de package (`@warah/backend`, `@warah/frontend`), projet Angular (`warah-frontend`), utilisateur système Docker, service de logs, tous les fichiers de contexte/doc. Le nom du dossier racine du projet sur le disque (`DINAWA`) et le nom du projet Supabase n'ont volontairement pas été touchés (changement structurel risqué / hors périmètre fichiers) — à faire séparément si souhaité.

**Problème trouvé, contourné pour le dev (2026-07-04) :** `warah.tg` n'est pas un domaine vérifié sur le compte Resend utilisé. `RESEND_FROM_EMAIL` est passé à `noreply@lbscallcenter.com` dans `.env` local (domaine déjà vérifié sur ce même compte, autre projet) — permet d'envoyer à n'importe quel destinataire réel pendant le développement. Piste explorée puis écartée : `onboarding@resend.dev` (adresse de test Resend, aucune vérification requise) ne fonctionne QUE si le destinataire est l'email du compte Resend lui-même (`bankatimabibe@gmail.com`) — vers tout autre destinataire, Resend rejette avec une 403 `validation_error`, invisible dans le dashboard (requête rejetée avant tout log). **À remplacer par `noreply@warah.tg` dès que ce domaine sera vérifié — ne jamais utiliser `lbscallcenter.com` en production.**

**Last completed :** Unité 13 Photos et documents du bien (2026-07-07) — voir détail dans Progress ci-dessous.

~~**Dépendance restante à câbler à l'unité 31**~~ — résolu 2026-08-07 : `AccountActivationService.reactivateIfEligible(userId)` est désormais aussi appelé par `MandatesService.accept()` (voir unité 31 ci-dessous), sur le même modèle fire-and-forget que `PropertiesService.create()`.

**Bug d'architecture trouvé et corrigé pendant les tests en conditions réelles de l'unité 12** : un compte `SUSPENDED_INACTIVITY` ne pouvait jamais se débloquer lui-même — `SupabaseAuthGuard` rejetait `POST /properties` (méthode mutante) avec `403 ACCOUNT_SUSPENDED` avant même d'atteindre `PropertiesService.create()`, empêchant tout appel à `reactivateIfEligible()`. Corrigé par un nouveau décorateur `@AllowWhileSuspended()` (`src/common/decorators/allow-while-suspended.decorator.ts`), vérifié par `SupabaseAuthGuard` juste avant de lever l'exception ; appliqué à `POST /properties`, et désormais aussi à `POST /api/mandates/:id/accept` (unité 31, 2026-08-07) pour la même raison. N'affecte pas `SUSPENDED_ADMIN`, toujours rejeté en `401` en amont. Invariant mis à jour dans `architecture.md`.

**Last completed :** Unité 28 Publication d'annonces et page publique (2026-07-28) — voir détail complet dans Progress ci-dessous (Phase 7). Unités 29-30 **réduites de périmètre** au `/architect` du même jour (pas de formulaire de contact, contact via lien WhatsApp construit côté frontend ; suspension admin/signalement reportés à l'existence du module `admin`). Phases 1-4 entièrement terminées (unités 01-22 ; unité 15 révisée le 2026-07-25 pour fusionner la création du bail dans l'invitation locataire ; unités 17-18 Cashpay explicitement reportées ; unité 23 partiellement construite, export PDF/XLSX reporté). 267 tests unitaires au total, build et lint propres. **Testé de bout en bout en conditions réelles** (2026-07-28, backend lancé localement sur le port 3001 avec la vraie base Supabase, port 192.168.1.70:3000 habituel injoignable depuis l'environnement sandboxé) : compte propriétaire réel créé et vérifié (vraie CNI, OCR passé en `VERIFIED`), bien créé → annonce publiée automatiquement, locataire invité/bail créé → annonce désactivée (ancien slug en 404), bail résilié → nouvelle annonce republiée (nouvel id, historique préservé), détail d'annonce avec contact résolu, filtres et validations (400/404) vérifiés. Toutes les données de test supprimées après validation (lignes DB, comptes Supabase Auth, photos CNI en storage).

**Last completed :** Unités 24-25 Rappels d'échéance et alertes d'impayés (2026-07-28, `/architect` le même jour) — voir détail dans Progress ci-dessous (Phase 5). Aucune migration nécessaire : `PaymentScheduleEntry.reminderSentAt`/`overdueAlertSentAt` et les templates/événements `payment-reminder`/`overdue-alert` (email + push) existaient déjà, posés par anticipation dès les unités 02 et 04. 21 tests unitaires ajoutés (`reminders.task.spec.ts`, `overdue.task.spec.ts`, nouveaux) — 280 tests au total, build et lint propres. Testé en conditions réelles (contexte NestJS réel via `createApplicationContext`, vraie base Supabase, script jetable supprimé après validation) : rappel envoyé pour une échéance à J-3 (repli sur le défaut propriétaire, le bail n'ayant pas de réglage propre), alerte envoyée et échéance passée à `OVERDUE` pour une échéance en retard de 6 jours, échéance lointaine (J+40) laissée intacte, deuxième exécution des deux crons sans aucun renvoi (anti-doublon confirmé par relecture des `reminderSentAt`/`overdueAlertSentAt`, inchangés).

**Trouvaille à traiter séparément (frontend)** : `dashboard.service.ts` (propriétaire) calcule le KPI "impayés" via `paiementsService.list({ status: 'OVERDUE' })` — mais `OVERDUE` y est un statut de `Payment`, jamais posé par aucun code actuel. Le nouveau statut `OVERDUE` des unités 24-25 est posé sur `PaymentScheduleEntry`, un modèle différent. Ce KPI reste donc à 0 même après cette passe — à corriger dans une prochaine session frontend (interroger les échéances en retard, pas les paiements).

**Last completed (hors plan, non documenté à l'époque) :** Réconciliation frontend Next.js ↔ backend NestJS (2026-08-04, commit `9064988`, session différente) — retrait complet de l'unité 07 (voir Current Status et notes sous les unités 07-09 ci-dessus), extension de `GET /api/payments` et `GET /api/payments/:id/receipt.pdf` au rôle `TENANT`, correctif `DATABASE_URL` (pooler Supabase EU/IPv4 + mot de passe URL-encodé), correctif extraction texte PDFKit (`WinAnsiEncoding`) dans `receipt-pdf.service.ts`. Documenté rétroactivement le 2026-08-06 après audit de cohérence code ↔ tracker (voir Current Status) — ce fichier n'avait pas été mis à jour au moment du changement.

**Last completed :** Unité 31 Mandats et espace gestionnaire (2026-08-07, `/architect` le même jour) — voir détail complet dans Progress ci-dessous (Phase 8).

**Last completed :** Unité 32 Tableau de bord gestionnaire (2026-08-08, `/architect` le même jour) — voir détail complet dans Progress ci-dessous (Phase 8). **Revue `/code-review` multi-agents le même jour** (3 agents en parallèle sur l'unité 31+32 combinées) a trouvé et corrigé une **fuite de confidentialité réelle et exploitable**, invisible avant cette session car aucune ligne `Mandate` réelle n'existait en base jusqu'ici — voir détail dans Progress, unité 32.

**Last completed :** Unité 33 Rapports mensuels automatiques (2026-08-08, `/architect` le même jour) — voir détail complet dans Progress ci-dessous (Phase 8).

**Last completed :** Unité 34 Profil public et avis (2026-08-08, `/architect` le même jour) — voir détail complet dans Progress ci-dessous (Phase 8). **Phase 8 (Gestionnaires immobiliers) intégralement terminée.** Migration `20260730085655_remove_identity_verification`, en attente depuis le 2026-08-06, appliquée à cette occasion (confirmé sans risque avec le développeur).

**Décisions du développeur (2026-08-06)** : (1) **CNI abandonnée définitivement** — le retrait du 2026-08-04 est confirmé comme un choix assumé, pas un oubli à corriger ; aucune vérification d'identité n'est prévue en V1. Question ouverte correspondante fermée (voir Open Questions). (2) **Cashpay (unités 17-18) reste reporté** — les agréments nécessaires (mobile money T-Money/Flooz) ne sont pas encore obtenus côté produit ; aucune date de reprise connue, bloquant indépendant du code. Le développeur choisit de continuer sur le reste du plan en attendant.

**Last completed :** Unité 35 Forfaits et quotas (2026-08-08/09, `/architect` le 2026-08-08) — voir détail complet dans Progress ci-dessous (Phase 9). **Unité 36 (prélèvement mensuel) explicitement reportée** — dépendance Cashpay déjà bloquée produit, décision confirmée par question à choix avant tout `/architect` sur la Phase 9. Script de rétro-remplissage (`scripts/backfill-subscriptions.js`) exécuté sur la vraie base le 2026-08-09 (7 comptes mis à jour). 345 tests au total, `tsc`/`eslint` propres.

**Next :** Phase 9 terminée pour son unique unité constructible (35 ; 36 reste bloquée par Cashpay). Prochain choix à faire avec le développeur : Phase 6 (tableaux de bord propriétaire/locataire — déjà couverte côté frontend par composition client, voir `dashboard.service.ts`, donc moins prioritaire côté backend) ou Phase 10 (unité 37, terminer `AdminModule`, resté très partiel). Corrections mineures toujours en attente, à caser à l'occasion : 1 erreur ESLint (`account.controller.spec.ts`) + 4 warnings (module admin) trouvées lors de l'audit du 2026-08-06, non corrigées.

---

## Progress

### Phase 1 — Fondations

- [x] 01 Setup projet NestJS, Prisma et Supabase — 2026-07-01 — `src/main.ts`, `src/app.module.ts`, `src/config/env.validation.ts`, `src/config/logger.config.ts`, `src/prisma/prisma.service.ts`, `src/prisma/prisma.module.ts`, `src/modules/health/health.controller.ts`, `src/modules/health/health.module.ts`, `prisma/schema.prisma`, `Dockerfile`, `railway.json`, `.env.example`, `eslint.config.mjs`, `.prettierrc`
- [x] 02 Schéma Prisma initial — 2026-07-01 — `prisma/schema.prisma` (21 modèles, 19 énums), `prisma/migrations/20260701104000_init_schema/`, `prisma/migrations/20260701104113_partial_unique_active_constraints/`. Migrations appliquées sur Supabase réel. `SUPABASE_JWT_SECRET` rendu optionnel dans `env.validation.ts` (validation JWT via `auth.getUser()`, pas de vérification locale HS256). Fix générique : les variables optionnelles vides dans `.env` sont normalisées en `undefined` avant validation (évitait un crash sur `CASHPAY_API_URL=`).
- [x] 03 Authentification Supabase et rôles — 2026-07-01 — `src/common/guards/supabase-auth.guard.ts`, `src/common/guards/roles.guard.ts`, `src/common/decorators/{public,roles,current-user}.decorator.ts`, `src/common/permissions/property-access.ts`, `src/common/types/{authenticated-user.type,express.d}.ts`, `src/modules/supabase/{supabase-admin.service,supabase.module}.ts`, `src/modules/auth/{auth.controller,auth.service,auth.module}.ts`. `@supabase/supabase-js` ajouté aux dépendances.
- [x] 04 Service emails transactionnels (Resend) — 2026-07-01 — `src/modules/email/email.service.ts`, `src/modules/email/email.module.ts`, `src/modules/email/templates/*` (layout + 11 templates + registry). Testé sur Resend réel (`delivered`). Domaine `warah.tg` non encore vérifié sur Resend — voir Questions en suspens.
- [x] 05 Service de stockage (Supabase Storage) — 2026-07-01 — `src/modules/storage/storage.service.ts`, `src/modules/storage/storage.module.ts`, `src/common/constants.ts`. Testé de bout en bout sur Supabase réel.
- [x] 06 Préférences de notifications et infrastructure push web — 2026-07-01 — `src/modules/push/{web-push.service,push.controller,push.module}.ts`, `src/modules/push/dto/{subscribe-push,unsubscribe-push}.dto.ts`, `src/modules/notify/{notify.service,notify.module,notification-events}.ts`. Testé en navigateur réel (notification reçue). Revue post-implémentation : 4 corrections appliquées (échec silencieux, bug multi-appareils, timeout/retry push, `take` manquant).

### Phase 2 — Comptes utilisateurs

- [ ] 07 Vérification automatique de la carte d'identité togolaise — **construite le 2026-07-04, intégralement retirée le 2026-08-04** (commit `9064988`, « simplifie l'auth ») : module `src/modules/identity/` supprimé en totalité (service, controller, listener, events, tests), modèle Prisma `IdentityVerification` et énum `IdVerificationStatus` retirés du schéma (migration `remove_identity_verification`), helper `assertIdentityVerified()` et son appel dans `PropertiesService.create()` supprimés, dépendances `tesseract.js`/`mrz` retirées de `package.json`. Plus aucune trace dans `src/` (vérifié par recherche globale le 2026-08-06). Détail historique de la construction originale conservé ci-dessous pour mémoire — **ne reflète plus l'état du code**.
      ~~2026-07-04 — `src/modules/identity/identity.controller.ts`, `identity.service.ts`, `identity-verification.service.ts` (OCR pur, `verifyFront`/`verifyBack`), `identity-verification.listener.ts` (post-traitement asynchrone), `identity-verification.events.ts`, `identity.module.ts`, branché dans `app.module.ts`.~~ `prisma/schema.prisma` : champ `updatedAt` + migration `20260704090000_identity_verification_pending_unique` (index unique partiel une seule ligne `PENDING` par utilisateur). Pattern du numéro de CNI et marqueurs texte figés à partir d'un exemplaire réel. `@types/multer` ajouté aux devDependencies (typage `Express.Multer.File`, nécessaire pour `FileInterceptor`).
      **Durcissement verso (même jour)** : `POST /identity/verify` accepte désormais deux images obligatoires (`image` recto + `imageBack` verso, `FileFieldsInterceptor`). Le verso est OCRé (même pipeline 4-rotations) pour extraire la ligne « Personne à prévenir » (nom + téléphone togolais 8 chiffres, dernier groupe trouvé sur la ligne) — donnée à forte valeur métier, jamais gate sur `VERIFIED`/`REJECTED`. Zone MRZ (format TD1) localisée par heuristique (lignes 25-35 car., ≥3 `<`) et validée via la librairie `mrz` (checksums ICAO 9303) — signal secondaire uniquement, stocké dans `mrzChecksumValid`, n'influence jamais la décision. Migration `20260704180000_identity_verification_back_side` : `storagePathBack`, `rawTextBack`, `emergencyContactRaw`, `emergencyContactPhone`, `mrzChecksumValid` (tous nullable, rétrocompatible). `mrz` ajouté aux dépendances + `code-standards.md`. Jest `transformIgnorePatterns` ajusté (`mrz` est ESM-only, nécessite d'être transformé par `ts-jest` contrairement au reste de `node_modules`).
      35 tests unitaires (3 fichiers `.spec.ts`, dont fixtures MRZ à checksum vérifié manuellement avec la lib avant écriture des tests). Testé en conditions réelles à chaque étape (app démarrée, vraie base Supabase, vrai utilisateur de test, curl + Postman) — voir Décisions pour les 2 bugs critiques trouvés et corrigés en cours de route (crash process sur image corrompue, rejet systématique sur CNI rotée). **Validé de bout en bout par l'utilisateur final avec sa vraie CNI recto+verso** (Postman) — résultat confirmé correct.
- [x] 08 Inscription propriétaire — 2026-07-04 — `src/modules/auth/auth.controller.ts` (`POST /api/auth/signup/owner`, `@Public()`, `FileFieldsInterceptor`), `auth.service.ts` (`signupOwner`), `dto/signup-owner.dto.ts`, `auth.module.ts` (importe `IdentityModule`). `src/common/guards/supabase-auth.guard.ts` étendu : rejet `403 EMAIL_NOT_CONFIRMED` si `email_confirmed_at` est `null` (modification partagée, impacte tous les endpoints authentifiés). `FRONTEND_URL` ajouté à `env.validation.ts`/`.env`/`.env.example`. Un seul appel Supabase (`generateLink({ type: 'signup' })`) crée le compte ET renvoie le lien de confirmation ; ce lien est envoyé via le template WARAH `signup-confirmation` existant (Resend), jamais l'email par défaut de Supabase — zéro endpoint de vérification côté NestJS, Supabase héberge et valide le lien lui-même. Rollback explicite du compte Supabase (`admin.deleteUser`) si la transaction Prisma (`User`+`OwnerProfile`) échoue après coup, pour éviter un compte orphelin bloquant. La vérification CNI est déclenchée en réutilisant directement `IdentityService.verify()` (même transaction logique que l'étape 07, pas de second appel HTTP). 17 tests unitaires (`auth.service.spec.ts`, `supabase-auth.guard.spec.ts`). Testé en conditions réelles : signup complet (`201`), email envoyé (échoue à la livraison — domaine `warah.tg` toujours non vérifié sur Resend, comportement attendu et déjà documenté, jamais bloquant), `sign-in` refusé par Supabase tant que l'email n'est pas confirmé, confirmation manuelle simulée puis `GET /auth/me` fonctionnel. Comptes de test supprimés après validation.
      **Mise à jour 2026-08-06 (retrait unité 07)** : `signup/owner` n'accepte plus de fichiers CNI (`FileFieldsInterceptor` retiré, DTO JSON pur) et n'appelle plus `IdentityService.verify()` — cette phrase de la description originale ne s'applique plus. `PropertiesService.create()` (unité 12) n'est plus gaté par un statut de vérification. Voir note de retrait sous l'unité 07 ci-dessus.
- [x] 09 Inscription locataire et gestionnaire — 2026-07-04 — `src/common/utils/invitation-token.ts` (token HMAC-SHA256 autoporteur, signature + expiration 7 jours, aucune dépendance nouvelle), `INVITATION_TOKEN_SECRET` ajouté à `env.validation.ts`/`.env`/`.env.example`. `dto/{invite-tenant,set-tenant-password,signup-manager}.dto.ts`. `auth.service.ts` : `inviteTenant`, `completeTenantSignup`, `signupManager`, plus `createConfirmedAccount()` factorisé (partagé par `signupOwner`/`signupManager`) et `mapDuplicateError()` (distingue email vs téléphone via `error.meta.target`). `auth.controller.ts` : `POST /auth/invite/tenant` (`@Roles(OWNER, MANAGER)`, authentifié), `POST /auth/signup/tenant?token=...` (`@Public()`), `POST /auth/signup/manager` (`@Public()`, `FileFieldsInterceptor` avec `referenceDocuments` optionnel jusqu'à 5 fichiers). Migration `20260704200000_manager_reference_documents` : `ManagerProfile.referenceDocumentPaths String[]`.
      **Décision majeure prise avec le développeur, différente du build-plan initial** : le compte locataire est créé dès `POST /invite/tenant` (Supabase `email_confirm: true` d'office — l'invitation vaut confirmation) avec toutes les infos fournies par l'inviteur (nom, prénom, téléphone, email) ; `POST /signup/tenant?token=...` ne fait plus que poser le mot de passe, aucun formulaire pour le locataire. La CNI locataire reste entièrement hors de ces deux endpoints — ~~ajoutable plus tard via `POST /identity/verify` (déjà utilisable par tout rôle authentifié)~~ _(cet endpoint n'existe plus depuis le retrait de l'unité 07, 2026-08-04)_.
      **Découverte importante** : le template email `tenant-invitation` (construit à l'étape 04) attend `propertyAddress` — l'invitation est donc rattachée à un `propertyId` précis, vérifié via `canActOnProperty()` (lecture directe de `Property`, sans dépendre du CRUD Phase 3 pas encore construit). Testé en conditions réelles avec un bien seedé manuellement (aucun endpoint de création de bien n'existe encore) : invitation → activation → connexion locataire → `GET /auth/me` fonctionnels de bout en bout ; inscription gestionnaire (`201`, `referenceDocumentPaths` vide si aucun document fourni) testée également, serveur stable. 32 tests unitaires (`auth.service.spec.ts` étendu, `invitation-token.spec.ts`). Comptes et bien de test supprimés après validation.
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé et corrigé 4 problèmes (Mode 1 — corrections ciblées, rien de systémique) : (1) **crash `500` non géré** sur `POST /signup/tenant` sans `?token` — `@Query('token')` n'est qu'une annotation TypeScript, aucune garde runtime ; `verifyInvitationToken()` faisait `.split('.')` sur `undefined`. Corrigé par une garde explicite en tête de fonction, vérifié en direct (`400` propre au lieu de `500`). (2) **chemin de stockage des références gestionnaire non sécurisé** — utilisait `pending/${Date.now()}-${originalname}` (nom de fichier utilisateur non filtré + collision possible) au lieu de la convention `${userId}/${randomUUID()}.${ext}` déjà établie dans le module identity. (3) **documents de référence orphelins** — uploadés avant la création du compte, sans rollback si celle-ci échouait ensuite ; réordonné pour uploader après coup, avec le vrai `userId`. (4) **échec trompeur** — `signupOwner`/`signupManager` ne vérifiaient que la présence des fichiers CNI, pas leur taille, avant de créer le compte ; un fichier de 6-9 Mo passait l'intercepteur (limite 10 Mo côté gestionnaire) puis échouait après coup dans `IdentityService.verify()` (limite 5 Mo), laissant un compte confirmé mais aucune vérification, et un retry avec le même email échouant en `409` déroutant. Corrigé par une validation de taille en amont (`assertCniFiles()`). Les 3 cas retestés en conditions réelles (token absent, nom de fichier malveillant `../../etc/passwd.jpg`, image de 6 Mo côté gestionnaire) — tous confirmés corrigés, aucun compte orphelin créé. 6 tests unitaires ajoutés/adaptés (72 tests au total sur le backend).
      **Révision inscription owner/manager (`/architect`, 2026-07-16)** — trois changements décidés avec le développeur, à l'occasion d'un constat plus large : ni les endpoints `signup/owner`/`signup/manager` ni la vérification CNI n'étaient reliés à un écran frontend réel (le formulaire public existant est une maquette, `setTimeout` sans appel API). (1) **CNI redevenue facultative à l'inscription** — `assertCniFiles()` supprimé, `identityService.verify()` n'est appelé que si `files.image` est fourni ; en contrepartie, `PropertiesService.create()` (unité 12) refuse désormais toute création de bien tant que `idVerificationStatus !== VERIFIED` via le nouveau helper `assertIdentityVerified()` (`src/common/permissions/identity-verified.ts`) — c'est le seul endroit où la vérification devient bloquante. (2) **`phone`/`city` ajoutés sur `User`** (pas sur `OwnerProfile`/`ManagerProfile` — aucune logique métier attachée à la ville, contrairement à `residenceCountry`), obligatoires dans `SignupOwnerDto`/`SignupManagerDto`. (3) **Document de référence gestionnaire retiré** — `referenceDocuments` (upload), `ManagerProfile.referenceDocumentPaths` (colonne, migration `20260716090000_user_city_and_manager_reference_removal`), et le code d'upload associé dans `signupManager()` sont supprimés ; le bucket `manager-documents` reste défini mais n'est plus utilisé. `GET /api/auth/me` enrichi de `identityVerifiedAt` (dérivé du `updatedAt` de la dernière `IdentityVerification` `VERIFIED`, jamais stocké en double) — badge de vérification visible uniquement par l'utilisateur lui-même pour l'instant. Principe retenu pour l'unité 31 (mandats, non construite) : accepter un mandat ne nécessitera pas de vérification d'identité propre au gestionnaire, la confiance transitant par le propriétaire déjà vérifié. **Bug latent découvert et corrigé en cours de route** : `apps/backend/tsconfig.json` n'avait aucune restriction `types`, donc `@types/jasmine` (installé côté `apps/frontend` lors du merge du 2026-07-12, hoisté en racine par les workspaces npm) était ramassé par TypeScript et entrait en collision avec `@types/jest` (`describe`/`expect` redéfinis, 130 erreurs de build sans rapport avec ce chantier) — corrigé en ajoutant `"types": ["node", "jest", "express", "multer", "supertest", "web-push", "pdfkit"]`. 8 tests unitaires ajoutés (`identity-verified.spec.ts` nouveau, `properties.service.spec.ts` étendu), suite `auth.service.spec.ts` largement réécrite (211 tests au total sur le backend). Testé en conditions réelles : signup owner/manager sans CNI (`201`, `identityVerification: null`), `POST /properties` refusé (`403`) tant que non vérifié, `GET /auth/me` reflète `identityVerifiedAt` avant/après. Comptes et bien de test supprimés après validation.
      **Tout ce paragraphe est obsolète depuis le 2026-08-04** (retrait complet de l'unité 07, voir plus haut) : `assertIdentityVerified()` n'existe plus, `PropertiesService.create()` ne bloque plus rien, `GET /auth/me` ne renvoie plus `identityVerifiedAt`. Conservé ici pour l'historique de la décision produit, pas pour décrire le comportement actuel.
      **Retrait du travail distant du compagnon (2026-07-25)** — décision explicite du développeur : les modules `admin`, `delegation` (+ `DelegationGuard`, `PowerDelegation`), `exports` et `listings` (version simplifiée du compagnon, en routes françaises `annonces`) ajoutés par un pull externe sont **entièrement supprimés** — hors-plan pour la délégation, très en retrait de la spec pour les trois autres (voir discussion produit du même jour). Retour à Resend pour l'envoi d'email (`email.service.ts`, `auth.service.ts` : `generateLink()` restauré) malgré le blocage connu (domaine `warah.tg` non vérifié) — décision explicite du développeur, en connaissance de cause. Garde-fou `assertValidTransition()` sur `OCCUPIED` restauré dans `PropertiesService` (avait été retiré par le pull, laissé signalé sans être corrigé jusqu'ici). **Conservé du pull** (jugé indépendant du débat produit) : réveil + retry automatique de Supabase dans `PrismaService` (la base gratuite s'endort et cassait les connexions), le cache d'URLs signées (`StorageService.getSignedUrls`/`invalidateCachedUrl`), et `GET /api/tenants`+`GET /api/tenants/:id` (lecture seule, ne contredit pas la décision de l'unité 14). `NotifyController` (`GET /api/notifications`, `GET /api/notifications/unread-count`) également conservé, nettoyé des références aux événements supprimés (`delegation-granted/revoked`, `listing-contact`). Migration `20260723172327_add_power_delegation` supprimée — vérifié qu'elle n'avait jamais été appliquée à la vraie base Supabase avant suppression. 211 tests passent toujours (aucune régression, la suite `auth.service.spec.ts`/`properties.service.spec.ts` d'avant le pull était déjà correcte). Testé en conditions réelles : signup Resend sans crash, `PATCH` direct vers `OCCUPIED` refusé (`400`), `GET /tenants` fonctionnel. **Décisions produit prises le même jour, à construire plus tard** (voir `build-plan.md` unités 28-29, révisées) : annonces publiées automatiquement dès qu'un bien passe `VACANT` (fini la publication manuelle), adresse exacte du bien visible publiquement sur l'annonce (décision assumée malgré le risque de sécurité physique signalé), formulaire de contact candidat remplacé par un lien WhatsApp pré-rempli (le numéro affiché est le même que celui du compte, pas de champ séparé) — `ListingContact` retiré du plan en conséquence.
- [x] 10 Sécurité d'accès et gestion du profil — 2026-07-06 — `src/modules/auth/auth.service.ts` : `login()` (compteur `failedLoginAttempts`/`lockedUntil` sur `User`, blocage 15 min après 5 échecs, message toujours générique), `requestPasswordReset()`/`confirmPasswordReset()` (branche enfin `PasswordResetOtp`, créé à l'étape 02 mais jamais utilisé jusqu'ici). `src/modules/supabase/supabase-admin.service.ts` étendu : `anonAuth` (client Supabase avec la clé anon, dédié à `signInWithPassword` — jamais la clé `service_role` pour une connexion utilisateur, moindre privilège). `src/modules/profile/` (nouveau module) : `ProfileController`/`ProfileService` — `GET/PATCH /api/profile`, `PATCH /api/profile/notification-consent`, `DELETE /api/profile`. `src/modules/storage/image-processor.ts` (nouveau) : `compressPhoto()`, premier usage réel de `sharp` dans le projet (pattern déjà documenté dans `library-docs.md`, jamais implémenté avant cette étape). Migration `20260705090000_login_lockout` : `User.failedLoginAttempts`/`lockedUntil`.
      **Décision qui déroge à un principe établi** : la connexion passe désormais par `POST /auth/login` (NestJS reçoit le mot de passe et appelle Supabase server-side via `anonAuth`) — jusqu'ici la connexion se faisait entièrement côté client. Nécessaire car Supabase n'expose aucun compteur d'échecs par compte ; décision prise explicitement avec le développeur après l'avoir signalée comme un renversement de principe.
      **6ᵉ bucket Storage `profile-photos`** créé sur le vrai projet Supabase — modifie l'invariant « cinq buckets uniquement » (`architecture.md`, `code-standards.md`) : aucun des cinq buckets existants ne correspondait à une photo de profil utilisateur.
      **Anonymisation (`DELETE /profile`)** : suppression définitive côté Supabase Auth (`admin.deleteUser`), ligne Prisma conservée avec champs personnels vidés (`email`/`phone`/`supabaseId` → `null`, nom → placeholder, `anonymizedAt` renseigné) — jamais de suppression SQL, historique `Payment`/`Lease` préservé.
      **Bug trouvé et corrigé pendant les tests en conditions réelles** : `sharp` lève une erreur brute (non-HTTP) sur une image corrompue — `Input buffer has corrupt header` — remontait en `500` au lieu d'un `400` propre (même famille de bug que le crash `tesseract.js` de l'étape 07). Corrigé par un `try/catch` autour de `compressPhoto()` dans `ProfileService.updateProfile()`, converti en `BadRequestException`. Reproduit puis corrigé en direct (image factice → `500`, puis `400` propre après correctif ; vraie photo générée avec `sharp` → upload réussi, `profilePhotoPath` en `.webp`).
      **Petit défaut trouvé et corrigé en cours de test** : la réponse de `login()` renvoyait l'instantané `User` pré-connexion (compteur d'échecs pas encore remis à zéro) au lieu de la valeur à jour — corrigé pour renvoyer l'utilisateur après remise à zéro.
      18 tests unitaires ajoutés (`auth.service.spec.ts` étendu : `login`/`requestPasswordReset`/`confirmPasswordReset` ; `profile.service.spec.ts` nouveau) — 90 tests au total sur le backend. Testé en conditions réelles de bout en bout : mauvais mot de passe → compteur incrémenté ; bon mot de passe → session + compteur remis à zéro ; reset OTP (code lu en base, ancien mot de passe invalidé, nouveau fonctionnel) ; `GET/PATCH /profile` (texte + vraie photo) ; `PATCH /profile/notification-consent` ; `DELETE /profile` sur un compte jetable dédié (jamais le compte de test réutilisable) — compte Supabase bien supprimé, ligne Prisma anonymisée et conservée. Toutes les données de test supprimées après validation.
      **Revue post-implémentation (`/review`)** a trouvé 3 problèmes ; 1 corrigé sur confirmation du développeur, 2 en attente. **Corrigé** : les anciennes photos de profil n'étaient jamais supprimées du Storage lors d'un remplacement (`PATCH /profile` avec nouvelle photo) ou d'une anonymisation (`DELETE /profile`) — viole l'invariant « suppression Storage effective quand la référence Prisma change » déjà appliqué aux photos de bien (étape 05/13). Corrigé dans `ProfileService.updateProfile()`/`anonymize()` avec un ordre volontaire upload/update-Prisma-puis-suppression-de-l'ancien (jamais l'inverse : si l'upload ou l'update échoue avant, le profil garde sa photo actuelle valide plutôt que de pointer vers un fichier supprimé). 5 tests unitaires ajoutés (95 tests au total). Vérifié en conditions réelles sur un compte jetable dédié : deux remplacements successifs de photo → seul le fichier le plus récent reste dans le bucket `profile-photos` ; anonymisation → bucket vide pour cet utilisateur, `profilePhotoPath` à `null` en base, ligne Prisma conservée, compte Supabase Auth supprimé. **En attente de décision du développeur** : (a) `anonymize()` ne change jamais `accountStatus` — un compte anonymisé reste `ACTIVE` et est indistinguable d'un compte actif dans toute requête future basée sur ce statut ; (b) aucun timeout explicite configuré sur les appels sortants vers l'API Admin Supabase (`signInWithPassword`, `updateUserById`, `deleteUser`, `createUser`, `generateLink`), alors que l'invariant « tout appel sortant a un timeout explicite » est déjà appliqué ailleurs (ex. `EmailService`, `WebPushService`).
- [x] 11 Blocage automatique des comptes inactifs — 2026-07-07 — `src/modules/scheduling/inactivity.task.ts` (`InactivityTask`, `@Cron(CRON_INACTIVITY)` = `0 7 * * *` UTC = 7h Lomé) — trois phases sous un même verrou Postgres (`pg_try_advisory_lock` via le nouveau helper `src/common/utils/advisory-lock.ts`, obligatoire pour ce job d'après `code-standards.md`) : réactivation en filet de sécurité, avertissements J-30/J-7/J-1, suspension à J-60. `src/modules/account/` (nouveau module) : `AccountActivationService.hasQualifyingActivity()`/`reactivateIfEligible()` — seule autorité pour décider si un `OWNER`/`MANAGER` a un bien non archivé ou un mandat actif ; `AccountController` → `GET /api/account/status`. Migration `20260707120019_inactivity_warning_fields` : 3 champs anti-doublon sur `User` (`inactivityWarning{30,7,1}SentAt`, remis à `null` à la réactivation). Nouveau template/événement `account-reactivated` (email + push), sur le modèle d'`account-suspended`. Constantes `CRON_INACTIVITY`/`INACTIVITY_SUSPENSION_DAYS`/`INACTIVITY_WARNING_DAYS` ajoutées à `src/common/constants.ts`.
      **Aucun changement nécessaire côté enforcement** : le blocage `403 ACCOUNT_SUSPENDED` sur les mutations pour `SUSPENDED_INACTIVITY` avait déjà été construit par anticipation à l'étape 10 (`SupabaseAuthGuard`) — retesté en direct pour confirmer qu'il tient toujours (lecture `200`, mutation `403`).
      **Dépendance différée, documentée explicitement** : le déblocage « immédiat » à la création d'un bien ou à l'acceptation d'un mandat (build-plan.md) ne peut pas encore être câblé — les modules `Property`/`Mandate` (unités 12, 31) n'existent pas. `AccountActivationService.reactivateIfEligible()` est prêt et testé isolément (voir ci-dessous), mais reste sans appelant réel jusqu'à ces unités ; le cron sert de filet de sécurité dans l'intervalle (voir Current Status).
      15 tests unitaires (`account-activation.service.spec.ts`, `account.controller.spec.ts`, `inactivity.task.spec.ts`) — 117 tests au total sur le backend. Testé en conditions réelles sur 5 comptes jetables avec `createdAt` rétrodaté directement en base (30/53/59/61 jours, plus un gestionnaire à 61 jours avec un mandat actif seedé à la main) : avertissements J-30/J-7/J-1 envoyés progressivement et cumulativement (vérifié champ par champ après exécution directe du cron via contexte NestJS), suspension automatique à J-61, gestionnaire avec mandat actif jamais averti ni suspendu. Réactivation testée en appelant `reactivateIfEligible()` directement après avoir créé un `Property` pour le compte suspendu (simulant ce que fera l'unité 12) : `accountStatus` repasse à `ACTIVE`, les 3 compteurs de rappel repassent à `null`. `GET /account/status` vérifié en HTTP réel pour un compte `SUSPENDED_INACTIVITY` (motif + condition de déblocage) et un compte `ACTIVE`. Tous les comptes, biens et mandats de test supprimés après validation.

### Phase 3 — Patrimoine immobilier

- [x] 12 CRUD biens immobiliers — 2026-07-07 — `src/modules/properties/` (nouveau module) : `PropertiesController`/`PropertiesService` — `POST/GET /api/properties`, `GET/PATCH/DELETE /api/properties/:id`. Toute action sur un bien existant passe par `canActOnProperty()` (étape 03, réutilisé tel quel, rien à modifier). Pagination offset-based (`page`/`limit`, plafond 100, enveloppe `{ data, page, limit, total }`) — premier précédent du projet pour un endpoint de liste, à réutiliser pour tous les futurs endpoints similaires.
      **Règles de transition de statut** (`assertValidTransition()` dans `PropertiesService`) : `VACANT ↔ RENOVATION` libre ; `→ OCCUPIED` interdit (piloté exclusivement par un futur bail, unité 15, pas encore construite — dépendance différée documentée, même pattern que l'étape 11) ; `OCCUPIED → VACANT` autorisé seulement si aucun `Lease` `ACTIVE` (vérifié directement sur la table Prisma, qui existe déjà depuis l'étape 02) ; `→ ARCHIVED` interdit via `PATCH` (uniquement via `DELETE`) ; `ARCHIVED` est terminal. `DELETE` (archivage logique, jamais de suppression physique) bloqué si un bail actif existe — message explicite invitant à résilier le bail d'abord ; la traçabilité du bail résilié (`status: TERMINATED`, jamais supprimé) est déjà garantie par le schéma existant.
      **Dépendance de l'étape 11 refermée** : `PropertiesService.create()` appelle `AccountActivationService.reactivateIfEligible(ownerId)` après chaque création — reste à faire de même pour `MandatesService.accept()` à l'étape 31.
      **Bug d'architecture trouvé et corrigé en conditions réelles** : un compte `SUSPENDED_INACTIVITY` ne pouvait jamais se débloquer lui-même — voir Current Status ci-dessus (nouveau décorateur `@AllowWhileSuspended()`).
      20 tests unitaires (`properties.service.spec.ts`) — 138 tests au total sur le backend. Testé en conditions réelles avec 3 comptes jetables (propriétaire, gestionnaire mandaté, tiers) + un bail seedé à la main : création (statut forcé `VACANT`, `ownerId` forcé au compte courant, `status` dans le body rejeté par le `ValidationPipe` global `forbidNonWhitelisted`), liste avec visibilité correcte par rôle (tiers ne voit rien, propriétaire voit ses biens, filtre `status`), toutes les transitions interdites/autorisées, blocage `PATCH`/`DELETE` avec bail actif puis déblocage après résiliation du bail, lecture-seule du propriétaire sous mandat actif vs mutation complète du gestionnaire mandataire, et bout-en-bout du fix `@AllowWhileSuspended()` (compte suspendu → `POST /properties` → `GET /account/status` confirme `ACTIVE`). Toutes les données de test supprimées après validation.
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé 2 problèmes "Important", les deux corrigés (Mode 1 — corrections ciblées) : (1) **duplication de la logique d'autorisation** — `PropertiesService.findAll()` réimplémentait à la main la règle de visibilité de `canActOnProperty()` (`ownerId` ou mandat actif) au lieu de la dériver d'une source unique, risque de divergence si `canActOnProperty()` évolue un jour. Corrigé en extrayant `propertyVisibilityWhere(user)` dans `src/common/permissions/property-access.ts`, co-localisé avec `canActOnProperty()` avec un commentaire croisé ; `findAll()` l'appelle désormais au lieu de reconstruire l'`OR` en dur. (2) **aucun `@MaxLength()` sur les champs texte libres** (`address`, `neighborhood`, `city`, `description`) alors que `code-standards.md` documente explicitement `@MaxLength(200)` sur `address` dans ce DTO précis — gap présent dans tous les DTOs du projet depuis l'étape 08, corrigé uniquement ici (`CreatePropertyDto`/`UpdatePropertyDto` : 200 sur les champs courts, 2000 sur `description`) ; pas de sweep rétroactif sur les unités précédentes, à faire séparément si souhaité. 3 tests unitaires ajoutés (`property-access.spec.ts`, nouveau) — 141 tests au total. Vérifié en conditions réelles : adresse de 201 caractères rejetée (`400`), 200 caractères acceptée, liste toujours correcte après le refactor.
- [x] 13 Photos et documents du bien — 2026-07-07 — `PropertiesModule` étendu (pas de nouveau module — voir /architect) : `POST/DELETE /api/properties/:id/photos[/:photoId]`, `POST/GET/DELETE /api/properties/:id/documents[/:documentId]`. `FilesInterceptor` (premier usage du projet pour un champ multi-fichiers unique, `photos`/`documents`) plutôt que `FileFieldsInterceptor`. `PropertiesService.findOne()` étendu pour embarquer `photos: [{id, url, position}]` avec URLs signées — pas de `GET /photos` dédié (gap du build-plan résolu à l'architecture) ; `GET /properties` (la liste) n'embarque rien, pour rester légère.
      **Plafonds cumulatifs** (`MAX_PHOTOS_PER_PROPERTY=10`, `MAX_DOCUMENTS_PER_PROPERTY=20`, `src/common/constants.ts`) — comptés sur les lignes déjà en base, rejet total (`400`) si l'upload dépasserait le plafond, jamais d'acceptation partielle. Photos : compression WebP via `compressPhoto()` (étape 10, réutilisé à l'identique, calibré exactement selon le build-plan — rien à changer) ; toutes les photos du lot sont compressées avant la moindre écriture Storage/Prisma, pour qu'une photo corrompue au milieu du lot ne laisse jamais les précédentes déjà en place. Documents : jamais compressés (qualité d'origine), un `PropertyDocumentType` par appel (plusieurs fichiers du même type acceptés en un seul appel).
      **Suppression** (photo ou document) : Storage puis Prisma, jamais l'inverse — même principe que l'étape 10. `canActOnProperty().canMutate` requis pour tout upload/suppression, `canRead` suffit pour lire.
      15 tests unitaires ajoutés (`properties.service.spec.ts` étendu) — 156 tests au total sur le backend. Testé en conditions réelles sur un bien de test dédié : upload de 2 puis 7 photos supplémentaires (vraies images générées, compressées en WebP), `GET /properties/:id` les embarque bien avec URLs signées et positions correctes, plafond de 10 respecté (upload de 2 photos supplémentaires à 9 déjà présentes → `400` propre), suppression d'une photo confirmée à la fois en base et dans le bucket Storage réel (fichier physiquement absent après coup). Document PDF uploadé avec type `STATE_OF_PLAY`, listé avec URL signée (jamais le chemin brut), supprimé proprement. Un compte tiers testé : refusé (`403`) sur upload de photo et sur liste de documents. Toutes les données de test (comptes, bien, fichiers Storage) supprimées après validation.
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé 2 problèmes "Important", les deux corrigés (Mode 1 — corrections ciblées) : (1) **acceptation partielle silencieuse sur les documents** — `addDocuments()` uploadait et créait la ligne Prisma fichier par fichier dans la même boucle, sans validation préalable du lot ; un type MIME invalide sur le 2ᵉ fichier d'un lot de 3 laissait le 1ᵉʳ déjà écrit en Storage et en base, contrairement à `addPhotos()` qui compresse tout le lot avant la moindre écriture. (2) **validation du Content-Type trop tardive** — aucun `fileFilter` sur les `FilesInterceptor`, le type MIME n'était vérifié que dans `StorageService.assertValid()`, après bufferisation complète en mémoire ; avec les plafonds de cette unité (jusqu'à 20 fichiers de 10 Mo), un lot pouvait bufferiser jusqu'à ~200 Mo avant la moindre validation. **Un seul correctif résout les deux problèmes** : `createMimeTypeFilter(bucket)` (nouveau, `src/modules/storage/mime-type-filter.ts`, s'appuyant sur `isAllowedMimeType()` exporté de `storage.service.ts` — seule source de vérité) branché comme `fileFilter` Multer sur les deux interceptors. Un rejet de type MIME fait désormais échouer toute la requête multipart _avant_ que le controller ne soit jamais appelé — aucun fichier du lot n'atteint le service, donc plus aucune écriture partielle possible. 3 tests unitaires ajoutés (`mime-type-filter.spec.ts`, nouveau) — 159 tests au total. Vérifié en conditions réelles : lot mixte (1 PDF valide + 1 `.docx` invalide) → `400` immédiat, zéro document créé en base _et_ zéro fichier dans le bucket Storage (même le PDF valide n'a pas été écrit) ; chemin heureux (photo et document valides seuls) toujours fonctionnel après le correctif.
- [x] 14 Locataires — 2026-07-09 — **Décision majeure prise avec le développeur, différente du build-plan initial** : pas de nouveau `POST /api/tenants` — le flux de création+invitation de l'unité 09 (`POST /api/auth/invite/tenant`) reste l'unique chemin, un locataire existe une seule fois dans le système (`User(role=TENANT)` + `TenantProfile`), jamais dupliqué par propriétaire. L'unité 14 construit trois choses : le blocage locataire↔bien, les deux historiques, et la contrainte "un seul bail actif par locataire".
      **Blocage locataire↔bien** (nouveau modèle `TenantPropertyBlock` : `propertyId`, `tenantUserId`, `blockedByUserId`, `reason` obligatoire, `@@unique([propertyId, tenantUserId])`) — jamais global au compte, jamais à l'échelle du propriétaire : un locataire bloqué sur un bien reste invitable par ce même propriétaire pour un autre bien, ou par n'importe quel autre propriétaire. Nouvelle fonction `assertTenantNotBlocked()` (`src/common/permissions/tenant-block.ts`), co-localisée dans le même esprit que `canActOnProperty()` — autorité unique, jamais de vérification inline ailleurs. Refusé (`409`) si un bail actif existe encore entre ce locataire et ce bien (résiliation d'abord requise) ou si déjà bloqué.
      **Vérifié dès l'invitation, pas seulement à la création du bail** — `AuthService.inviteTenant()` (unité 09) modifié : recherche l'utilisateur existant par email OU téléphone avant d'appeler Supabase, et rejette avec `403` + le motif exact si bloqué sur ce bien précis. Décision prise avec le développeur : plus clair et évite au propriétaire de perdre du temps à réinviter quelqu'un qu'il a explicitement écarté de ce bien.
      **Nouveau module `src/modules/tenants/`** : `TenantsService`, `PropertyTenantsController` (`POST /api/properties/:propertyId/tenants/:tenantUserId/block`, `GET /api/properties/:propertyId/tenants/history`), `TenantsController` (`GET /api/tenants/:tenantUserId/leases/history`, accessible aussi au locataire lui-même pour son propre historique — seul cas où `@Roles` inclut `TENANT` sur une route hors périmètre "son propre bail").
      **Historiques** — aucun bilan financier pour l'instant (module `Payment` pas encore construit, unités 16-23), mais forme de réponse pensée pour l'accueillir plus tard sans rupture de contrat : jamais de valeurs à zéro simulées. Autorisation de l'historique par locataire : le locataire lui-même, un admin, ou tout propriétaire/gestionnaire ayant eu au moins un bail (actif ou passé) avec ce locataire — refusé pour un tiers sans aucun lien (vie privée du locataire).
      **Contrainte "un seul bail `ACTIVE` par locataire"** — découverte en cours de route qu'elle existait **déjà** en base depuis l'unité 02 (migration `20260701104113_partial_unique_active_constraints`, index `leases_tenant_active_unique`) ; le commentaire du schéma disant "à ajouter manuellement" était resté par erreur après coup. Migration dupliquée détectée et corrigée avant application (voir Problèmes résolus).
      20 tests unitaires ajoutés (`tenant-block.spec.ts` nouveau, `tenants.service.spec.ts` nouveau, `auth.service.spec.ts` étendu) — 181 tests au total. Testé en conditions réelles sur des comptes et un bien de test dédiés (script Node jetable, données supprimées après validation) : invitation initiale réussie, historique du bien vide (cohérent, aucun bail possible avant l'unité 15), bail actif seedé manuellement pour simuler l'unité 15, blocage refusé pendant que le bail est actif, historique du bien et du locataire corrects une fois le bail renseigné, propriétaire étranger refusé sur l'historique du locataire (`403`), bail résilié puis blocage réussi, double blocage refusé (`409`), **ré-invitation du locataire bloqué sur ce bien refusée avec le motif exact dans le message (`403`)** — le scénario clé de cette unité.
      **Revue post-implémentation (`/review`, même jour)** a trouvé 2 problèmes "Important" et 2 "Mineurs", les quatre corrigés. (1) **Fuite de confidentialité sur `GET /api/tenants/:id/leases/history`** — `canAccessTenantHistory()` fonctionnait comme un portail tout-ou-rien : dès qu'une seule relation (bail actuel ou passé) existait entre l'appelant et le locataire, la requête renvoyait **tous** ses baux, y compris ceux liés à des propriétaires totalement étrangers à l'appelant. Corrigé en appliquant le même principe que `propertyVisibilityWhere()` (déjà établi à l'unité 12) : la relation devient un filtre sur la requête `Lease` elle-même (`OR: [{ownerId}, {property.mandates...}]`), jamais une simple porte d'entrée — un propriétaire/gestionnaire lié ne voit désormais que ses propres baux avec ce locataire, jamais ceux d'un tiers. Vérifié en conditions réelles avec 2 propriétaires sans aucun lien entre eux et 1 locataire partagé : le propriétaire A ne voit plus le bail du propriétaire B. (2) **Course concurrente non gérée sur le blocage** — deux appels simultanés sur le même `(propertyId, tenantUserId)` pouvaient tous deux passer la vérification applicative puis produire un `P2002` non intercepté (`500` brut). Corrigé par un `try/catch` mappant `P2002` en `409` propre, même réflexe que `mapDuplicateError()` dans `auth.service.ts`. (3) **Recherche d'utilisateur existant trop large** — `inviteTenant()` cherchait par email/téléphone sans filtrer `role: 'TENANT'` ; sans impact fonctionnel avéré mais corrigé par précision. (4) **Blocage contournable via un bail `EXPIRED`** — seul `ACTIVE` était vérifié avant blocage ; un bail expiré mais jamais formellement résilié (`TERMINATED`) laissait bloquer un locataire potentiellement toujours sur place. Corrigé : `status: { in: ['ACTIVE', 'EXPIRED'] }`, seul `TERMINATED` libère. 4 tests unitaires supplémentaires (185 au total).
- [x] 15 Baux simplifiés (V1) — 2026-07-10 — Nouveau module `src/modules/leases/` : `LeasesService`/`LeasesController` — `POST /api/leases` (création), `POST /api/leases/:id/terminate` (résiliation). `canActOnProperty().canMutate` requis pour les deux, mêmes rôles que la création de bien (`OWNER`, `MANAGER`, pas `ADMIN`).
      **Création** : transaction unique (`Lease` + `PaymentScheduleEntry[]` + `Property.status=OCCUPIED`) — cohérent avec `code-standards.md` qui cite explicitement ce cas. `assertTenantNotBlocked()` (unité 14) réutilisée telle quelle — referme le gap laissé ouvert : sans ça, un blocage locataire↔bien pouvait être contourné en créant directement un bail avec un `tenantId` existant, en évitant complètement le flux d'invitation. Contrainte "un seul bail `ACTIVE` par locataire" (déjà en base depuis l'unité 02) : violation `P2002` capturée et remappée en `409` propre.
      **Calendrier d'échéances** : généré immédiatement à la création (méthode privée `buildScheduleEntries()`, pas encore un service dédié — l'unité 16 décidera si elle doit devenir `PaymentSchedulerService`), sur la durée fixe (`endDate`) ou 12 mois glissants pour un bail ouvert. **Décision prise avec le développeur** : pas de cron dédié à la prolongation du calendrier glissant — génération à la demande, l'unité 16 complètera les échéances manquantes à la lecture. `dueDate` = début de chaque période, montant = `(monthlyRent + monthlyCharges) × nombreDeMoisDansLaPeriode`, pas de prorata sur la première période même si `startDate` ne tombe pas en début de mois calendaire.
      **Nouveaux champs `Lease.reminderDaysBefore`/`overdueAlertWindowDays`** (optionnels, `null` = pas de rappel/alerte configuré) — **décision prise avec le développeur, propres à chaque bail**, indépendants des champs globaux `User.reminderDaysBefore`/`overdueGraceDays` existants (unité 10). Stockage uniquement dans cette unité — l'envoi effectif des rappels/alertes reste pour les unités 24/25, pas encore construites.
      **Notification au locataire à la création** : via `NotifyService.notifyUser()` (nouvel événement `lease-created`, jamais un email direct — pas un email d'authentification, invariant #7) — nouveau template `lease-created.template.ts`, enregistré dans `template-registry.ts` et `notification-events.ts`. Une notification manquée (locataire sans email/push actif) ne fait jamais échouer la création du bail (`try/catch` + log, même réflexe que `AccountActivationService`).
      **Résiliation** : transaction unique — `Lease.status=TERMINATED` + `terminatedAt` + `terminationReason` (optionnel), `Property.status=VACANT`, et **purge des `PaymentScheduleEntry` strictement futures jamais touchées par un paiement** (`periodStart >= terminatedAt AND payments: none`) — évite de laisser des échéances `PENDING` fantômes sur un bail résilié ; les échéances passées ou en cours (même avec paiement partiel) restent intactes, conformément au build-plan ("quittances passées générables à la volée").
      `PropertiesService.assertValidTransition()` : message d'erreur obsolète mis à jour (ne dit plus "pas encore disponible" pour `OCCUPIED` — pointe maintenant vers `POST /api/leases`).
      Migration `20260710173321_lease_reminder_and_overdue_alert_settings`. 18 tests unitaires ajoutés (`leases.service.spec.ts`, nouveau) — 203 tests au total. Testé en conditions réelles sur des comptes/biens dédiés (script Node jetable, données supprimées après validation) : création de bail (3 échéances mensuelles générées, montants et `dueDate` corrects), bien passé à `OCCUPIED`, second bail actif refusé (`409`), locataire bloqué sur un bien refusé (`403`), propriétaire étranger refusé (`403`), résiliation (bien repassé à `VACANT`, échéances futures purgées, `409` sur double résiliation), nouveau bail possible pour le même locataire après résiliation du précédent.
      **Fusion bail↔locataire (`/architect` révision paiements, 2026-07-25)** — décision prise avec le développeur en même temps que le lancement de la Phase 4 : `POST /api/leases` (création) est retiré, ses champs absorbés dans `InviteTenantDto` (unité 09). `AuthService.inviteTenant()` gère désormais deux chemins : nouveau locataire (compte Supabase + `User`+`TenantProfile`+`Lease`+échéancier, email d'invitation) ou locataire déjà connu de la plateforme (ex. bail précédent résilié, nouveau bien — juste un nouveau `Lease`+échéancier dans la même transaction, aucun nouveau compte ni email, `invitationUrl` renvoyé `null`) — ce second chemin referme le gap qu'aurait laissé un simple retrait de `POST /api/leases` sans remplacement. `buildScheduleEntries()` déménagé en fonction pure dans `src/modules/leases/schedule-builder.ts`, partagée entre les deux chemins. `LeasesService`/`LeasesModule` ne couvrent plus que `terminate()` et le nouvel endpoint `GET /api/leases/:id/schedule` (accessible au locataire lui-même). `AuthService.mapDuplicateError()` étendu : la contrainte `leases_tenant_active_unique` (index partiel SQL brut, rapportée par Prisma dans `error.message` et non `meta.target`) est désormais vérifiée avant le cas générique email/téléphone, puisque la fusion peut désormais la heurter depuis ce point d'entrée. Tests réorganisés : `schedule-builder.spec.ts` (nouveau, logique pure d'échéancier), `leases.service.spec.ts` (réduit à `terminate`/`getSchedule`), `auth.service.spec.ts` étendu (création de bail + chemin locataire existant + mapping du conflit). 253 tests au total après cette réorganisation (avant l'ajout des unités 16-22 ci-dessous).

### Phase 4 — Paiements

- [x] 16 Modèle de paiements et générateur d'échéances — 2026-07-25 — voir détail dans build-plan.md unité 16 (révisée). `computeEntryStatus()` recalcule et persiste le statut d'échéance à chaque écriture de `Payment`, pas de dérivation à la volée en lecture.
- [ ] 17 Intégration Cashpay — initialisation de paiement — **reportée** (décision explicite du développeur, 2026-07-25) : Cashpay mis de côté pour continuer le développement sans dépendre de cette intégration externe. `PaymentMethod.TMONEY`/`FLOOZ` restent au schéma, inutilisables par aucun endpoint pour l'instant.
- [ ] 18 Webhook Cashpay — confirmation et idempotence — **reportée**, même décision. La garde « jamais confirmer/rejeter manuellement un `Payment` `source = CASHPAY_API` » est déjà posée par anticipation dans `PaymentsService.assertConfirmable()` (unité 20).
- [x] 19 Saisie manuelle par le propriétaire ou le gestionnaire — 2026-07-25 — voir détail ci-dessous
- [x] 20 Déclaration de paiement par le locataire — 2026-07-25 — voir détail ci-dessous
- [x] 21 Génération de quittance PDF à la volée — 2026-07-25 — voir détail ci-dessous
- [x] 22 Envoi automatique de quittance — 2026-07-25 — voir détail ci-dessous
- [ ] 23 Historique et export des paiements — **historique implémenté** (`GET /api/payments`, filtres + pagination, voir détail ci-dessous), **export PDF/XLSX reporté** (non demandé pour cette passe, `exceljs` retiré du projet lors du nettoyage du 2026-07-25 précédent)

**19-22 Paiements manuels, déclarations, quittances — 2026-07-25** — Construits ensemble dans la même session, sans Cashpay (unités 17-18 reportées, voir ci-dessus), suite à `/architect` (« Fonctionnalité paiements sans intégration Cashpay API »).

- `src/modules/payments/` (nouveau) : `PaymentsService`/`PaymentsController` — `POST /api/payments/manual` (saisie directe `PAID`, `source=MANUAL_OWNER`), `GET /api/payments` (historique paginé, filtres `propertyId`/`tenantUserId`/`status`/`source`/`from`/`to`), `POST /api/payments/:id/confirm`, `POST /api/payments/:id/reject`, `GET /api/payments/:id/receipt.pdf`. `computeEntryStatus()` recalcule `PaymentScheduleEntry.paidAmount`/`status` à chaque écriture (saisie manuelle ou confirmation). `assertConfirmable()` : refuse `confirm`/`reject` sur un `Payment` `source=CASHPAY_API` (anticipation unité 18) et sur tout `Payment` qui n'est pas `PENDING_CONFIRMATION`.
- `src/modules/payment-declarations/` (nouveau) : `PaymentDeclarationsService`/`PaymentDeclarationsController` — `POST /api/payment-declarations` (locataire uniquement, sur son propre bail), `PATCH /api/payment-declarations/:id` (montant/date/méthode/note **et justificatif**, tant que `PENDING_CONFIRMATION`), `DELETE /api/payment-declarations/:id` (annulation, cascade Prisma sur `PaymentDeclaration`). `PaymentDeclarationRemindersTask` (`@Cron('0 8 * * *')`, verrou `pg_try_advisory_lock`, même pattern que `InactivityTask`) — relance à J+3 et J+7 via `reminder3SentAt`/`reminder7SentAt` (migration `20260725123519_payment_declaration_reminders`).
- Nouveau helper `resolveResponsibleUserId()` (`src/common/permissions/property-access.ts`, co-localisé avec `canActOnProperty()`) — gestionnaire mandaté actif sinon propriétaire, utilisé pour notifier « qui peut agir sur ce bien » (déclaration, relance).
- Restriction volontaire non explicitée par le build-plan d'origine : `paymentMethod`/`declaredMethod` limités à `CASH`/`BANK_TRANSFER` sur ces deux flows (`TMONEY`/`FLOOZ` réservés à Cashpay, unité 17, non construite).
- **Template/événement manquant ajouté** : `payment-rejected` (email + push) n'existait pas dans le registre construit à l'unité 04 alors que le build-plan (unité 20) exige une notification au locataire sur rejet — `payment-rejected.template.ts` créé, enregistré dans `template-registry.ts` et `notification-events.ts`.
- `src/modules/receipts/` (nouveau) : `ReceiptPdfService` (PDFKit, gabarit sobre construit faute de maquette client — nom propriétaire/locataire, adresse, période, montant, date, mode, référence, source ; mention explicite de la période pour les baux non mensuels), `PaymentConfirmedListener` (écoute l'événement `payment.confirmed`, émis par `PaymentsService.createManual()`/`confirm()` — génère le PDF une seule fois, notifie propriétaire et locataire via `notifyUser()` avec pièce jointe, jamais stocké). `payment.events.ts` (`src/modules/payments/`) définit l'événement, typé, sur le modèle de `identity-verification.events.ts`.
- `GET /api/payments/:id/receipt.pdf` — `StreamableFile` NestJS (`res.set()` + `Content-Disposition`), premier usage de ce pattern dans le projet ; refuse (`409`) si le `Payment` n'est pas `PAID`.
- **Simplification assumée sur le canal de notification de quittance** : `NotifyService.notifyUser()` force déjà le canal email dès qu'une pièce jointe est fournie (contrat documenté dans son propre commentaire depuis l'unité 06) — `PaymentConfirmedListener` fournit systématiquement le PDF en pièce jointe, pas de branche "push avec lien, sans PDF" séparée à construire.
- 44 tests unitaires ajoutés (`payments.service.spec.ts`, `payment-declarations.service.spec.ts`, `receipt-pdf.service.spec.ts`, `payment-confirmed.listener.spec.ts`, `payment-declaration-reminders.task.spec.ts`, tous nouveaux) — 254 tests au total sur le backend, build et lint intégralement propres.
- **Testé en conditions réelles de bout en bout** (deux scripts Node jetables, données supprimées après validation) : (1) signup owner → login → vérification identité simulée `VERIFIED` → création de bien → invitation locataire + bail (12 échéances mensuelles générées) → saisie manuelle `PAID` → téléchargement quittance (PDF valide, en-tête `%PDF`) → historique paginé ; (2) signup + invitation → activation locataire (token d'invitation réel) → connexion locataire → déclaration de paiement → modification de la déclaration → confirmation par le propriétaire → téléchargement de la quittance confirmée → deuxième déclaration rejetée avec motif → troisième déclaration annulée par le locataire. Les deux scénarios ont validé silencieusement l'envoi de notification (`payment.confirmed` → `PaymentConfirmedListener`, aucune erreur dans les logs serveur).
- **Problème d'infrastructure résolu en cours de route** : `SUPABASE_SERVICE_ROLE_KEY` avait été rotée vers le nouveau format de clé Supabase (`sb_secret_...`, signature asymétrique) sans que la propagation côté Supabase soit encore stabilisée — plusieurs appels `admin.*` (SDK `@supabase/supabase-js`) ont échoué de façon intermittente avec `unrecognized JWT kid` pendant quelques minutes après la rotation, y compris sur des appels sans rapport avec ce chantier (`listUsers`, `updateUserById`). Confirmé comme un problème de propagation Supabase et non un bug applicatif (un appel `fetch` brut avec les mêmes headers réussissait pendant que le SDK échouait sur le même process) ; résolu de lui-même après quelques réessais espacés. Ne rien changer côté code si ce symptôme précis (`unrecognized JWT kid <nil> for algorithm ES256`) réapparaît juste après une rotation de clé — attendre la propagation.

### Phase 5 — Rappels et alertes automatiques

- [x] 24 Cron rappels d'échéance adaptés à la fréquence — 2026-07-28 — `src/modules/scheduling/reminders.task.ts` (`RemindersTask`, `@Cron(CRON_PAYMENT_REMINDERS)` = `0 * * * *`, horaire). Sélectionne les `PaymentScheduleEntry` `PENDING`/`PARTIAL` sans `reminderSentAt`, fenêtre candidate large (90j) filtrée précisément en mémoire selon le délai effectif (`Lease.reminderDaysBefore` prime, sinon repli sur `User.reminderDaysBefore` du propriétaire, défaut 5j). Notifie le locataire via `notifyUser()` (événement `payment-reminder`, template/push déjà existants depuis l'unité 04), pose `reminderSentAt`.
- [x] 25 Cron alertes d'impayés adaptés à la fréquence — 2026-07-28 — `src/modules/scheduling/overdue.task.ts` (`OverdueAlertsTask`, `@Cron(CRON_OVERDUE_ALERTS)` = `30 * * * *`, décalé de 30 min par rapport à l'unité 24 pour éviter que les deux crons ne tapent la base à la même minute — toujours horaire au sens du build-plan). Sélectionne les `PaymentScheduleEntry` `PENDING`/`PARTIAL` en retard sans `overdueAlertSentAt`, délai de grâce résolu `Lease.overdueAlertWindowDays` → repli `User.overdueGraceDays` (défaut 3j) ; passe l'échéance à `OVERDUE` (jamais posé ailleurs, voir `computeEntryStatus()` unité 16), notifie le responsable via `resolveResponsibleUserId()` (événement `overdue-alert`). **Anti-doublon strict décidé avec le développeur** : `overdueAlertSentAt` une fois posé, plus jamais renotifié pour cette échéance précise même en cas d'oscillation de statut après un paiement partiel.
      Les deux crons réutilisent `withAdvisoryLock()` (même garde que `InactivityTask`/`PaymentDeclarationRemindersTask`), enregistrés dans `SchedulingModule` (déjà `@Global()` via `NotifyModule`/`PrismaModule`, aucun import de module supplémentaire nécessaire). **Aucune migration** : `reminderSentAt`/`overdueAlertSentAt` sur `PaymentScheduleEntry` et les templates/événements `payment-reminder`/`overdue-alert` (email + push) existaient déjà, posés par anticipation aux unités 02 et 04. **Décision prise avec le développeur, hors périmètre de cette passe** : pas d'endpoint de lecture pour le frontend (liste des échéances en retard/à relancer) — uniquement l'envoi de notifications ; voir la trouvaille sur le KPI "impayés" du dashboard propriétaire dans Current Status.
      21 tests unitaires (`reminders.task.spec.ts`, `overdue.task.spec.ts`) — 280 tests au total. Testé en conditions réelles via `NestFactory.createApplicationContext()` (vraie base Supabase, script jetable) : rappel envoyé et `reminderSentAt` posé pour une échéance à J-3 (repli sur défaut propriétaire), alerte envoyée, statut passé à `OVERDUE` et `overdueAlertSentAt` posé pour une échéance en retard de 6 jours, échéance lointaine (J+40) intacte, deuxième exécution des deux crons sans aucun renvoi. Données de test supprimées après validation.

### Phase 6 — Tableaux de bord propriétaire et locataire

- [ ] 26 Tableau de bord propriétaire
- [ ] 27 Tableau de bord locataire

### Phase 7 — Annonces de biens vacants

- [x] 28 Publication d'annonces et page publique — 2026-07-28 — `src/modules/listings/{listings.service,listings.controller,listings.module,listings.service.spec,dto/list-public-listings-query.dto}.ts`. `ListingsService.publishForProperty()`/`deactivateForProperty()` branchés dans les 3 points d'accroche déjà transactionnels : `PropertiesService.create()` (publication à la création, bien toujours `VACANT`), `AuthService.createLeaseWithinTransaction()` (désactivation quand le bien passe `OCCUPIED`), `LeasesService.terminate()` (republication quand le bien repasse `VACANT`). Chaque publication crée une **nouvelle ligne `Listing`** (jamais de réactivation d'une ligne existante) pour conserver l'historique complet des activations/désactivations par bien. Slug unique généré (`slugify(quartier-type)` + suffixe aléatoire 6 caractères en cas de collision, jusqu'à 5 tentatives). Routes publiques `@Public()` : `GET /api/public/listings` (pagination + filtres type/ville/quartier/loyer/pièces, vignette signée), `GET /api/public/listings/:slug` (détail complet, toutes les photos signées, contact via `resolveResponsibleUserId()` — gestionnaire mandaté sinon propriétaire — 404 si annonce inconnue ou non `ACTIVE`). Aucune migration Prisma nécessaire (`Listing`, `ListingStatus` déjà présents depuis la migration initiale). 8 tests unitaires dédiés + assertions ajoutées dans `properties.service.spec.ts`/`leases.service.spec.ts`/`auth.service.spec.ts` sur les 3 points d'accroche. Build, lint et suite complète (25 suites, 267 tests) propres. **Testé de bout en bout en conditions réelles le 2026-07-28** (backend local port 3001, vraie base Supabase) : cycle complet création de bien → publication → invitation locataire/bail → désactivation → résiliation → republication (nouvelle ligne) validé avec un vrai compte propriétaire (CNI vérifiée par OCR réel). Données de test supprimées après validation.
- [ ] 29 Contact candidat locataire — **révisé au `/architect` du 2026-07-28** : pas de formulaire de contact ni de `ListingContact` (modèle Prisma conservé mais inutilisé) — le contact se fait via un lien WhatsApp pré-rempli affiché sur la page de détail, construit côté frontend à partir de `contactPhone` renvoyé par `GET /api/public/listings/:slug`. Aucun endpoint dédié à construire.
- [ ] 30 Modération et suspension automatique — **réduit de périmètre au `/architect` du 2026-07-28** : le cron de suspension à 90 jours sans contact a été retiré (contradictoire avec l'absence de tracking de contact décidée pour l'unité 29). `POST /api/admin/listings/:id/suspend` et `POST /api/public/listings/:id/report` reportés à la construction du module `admin` (Phase 10, actuellement inexistant).

### Phase 8 — Gestionnaires immobiliers

- [x] 31 Mandats et espace gestionnaire — 2026-08-07 (`/architect` le même jour) — nouveau module `src/modules/mandates/` : `MandatesService`/`MandatesController` (`POST /api/mandates`, `GET /api/mandates`, `POST /api/mandates/:id/accept`, `POST /api/mandates/:id/revoke`), `ManagerController` (`GET /api/manager/portfolio`), `ManagersController` (`GET /api/managers/search`). `GET /api/mandates` et `GET /api/managers/search` ne sont pas dans le build-plan d'origine — ajoutés parce que sans eux le gestionnaire n'a aucun moyen de découvrir un mandat `PENDING` à traiter, ni le propriétaire de retrouver un `managerId` pour composer `CreateMandateDto` (décision prise en cours de route, cohérente avec l'allocation « à trancher en cours de route, pas bloquant » du blueprint `/architect`).
      **Aucune migration nécessaire** : l'index unique partiel `mandates_property_active_unique` (`Mandate(propertyId) WHERE status='ACTIVE'`) existait déjà depuis la toute première migration du projet (`20260701104113_partial_unique_active_constraints`) — le schéma avait anticipé cette contrainte dès l'unité 02, jamais utilisée jusqu'ici.
      **Décisions prises avec le développeur (`/architect`)** : (1) le gestionnaire cible est désigné par `managerId` direct dans `CreateMandateDto` (pas par email/téléphone comme `inviteTenant`) — nécessite un endpoint de recherche séparé, d'où `GET /api/managers/search?email=|phone=` (au plus une correspondance, jamais un annuaire). (2) Un mandat ne porte jamais sur plusieurs biens de statuts mixtes ambigus — `CreateMandateDto.propertyIds` accepte un tableau (un même gestionnaire proposé sur plusieurs biens en un seul appel), mais produit une ligne `Mandate` distincte par bien, jamais une relation many-to-many sur une seule ligne. (3) Un bien ne peut jamais avoir deux mandats `ACTIVE` simultanés (confirmé avec le développeur) — appliqué à la fois en amont (vérification applicative dans `create()`) et en aval (contrainte DB déjà existante, capturée en `409` propre si `P2002`).
      **Création (`MandatesService.create()`)** : réservée au propriétaire réel des biens listés (`Property.ownerId === user.id`, jamais dérivé de `canActOnProperty()` qui couvrirait aussi un gestionnaire déjà mandaté sur ce bien — un mandat est un acte de propriété, pas un droit opérationnel délégable) ; tout ou rien sur le lot de biens. Rejette : auto-mandat, pourcentage > 100 (`feeType=PERCENTAGE`), gestionnaire introuvable ou non-`MANAGER`, gestionnaire `SUSPENDED_ADMIN` (ne pourra jamais se reconnecter pour accepter), bien non détenu par l'appelant, bien déjà sous mandat `ACTIVE`. Transaction unique (une ligne `Mandate` `PENDING` par bien). Notifie le gestionnaire une seule fois pour tout le lot (nouvel événement `mandate-created`) — échec de notification non bloquant (`try/catch` + log, même réflexe que toutes les unités précédentes).
      **Acceptation (`MandatesService.accept()`)** : réservée au gestionnaire destinataire, rejette si le mandat n'est plus `PENDING`. `@AllowWhileSuspended()` posé sur l'endpoint — referme la dépendance laissée ouverte aux unités 11/12 : c'est précisément cette action qui doit pouvoir réactiver un compte `SUSPENDED_INACTIVITY` (`AccountActivationService.reactivateIfEligible()` appelé en fire-and-forget après l'acceptation). `P2002` sur la contrainte unique partielle (deux mandats `PENDING` concurrents sur le même bien, un seul peut être accepté) capturé et remappé en `409` propre.
      **Révocation (`MandatesService.revoke()`)** : un seul endpoint sert à la fois au refus d'un mandat `PENDING` (par le gestionnaire) et à la révocation d'un mandat `ACTIVE` (par le propriétaire ou le gestionnaire), motif optionnel.
      **Transfert des droits opérationnels** : **aucune modification nécessaire** dans `src/common/permissions/property-access.ts` — `canActOnProperty()`/`resolveResponsibleUserId()`/`propertyVisibilityWhere()` lisaient déjà les `Mandate` `ACTIVE` depuis l'unité 03/12, par anticipation de cette unité.
      Nouveau template email `mandate-created.template.ts` (enregistré dans `template-registry.ts`/`notification-events.ts`) — portée volontairement limitée à cet unique événement : `mandate-accepted`/`mandate-revoked` déjà visibles par leurs auteurs respectifs via la réponse HTTP immédiate, pas de notification asynchrone ajoutée pour eux dans cette passe (à reconsidérer si le besoin apparaît).
      13 tests unitaires ajoutés (`mandates.service.spec.ts`, nouveau) — 278 tests au total sur le backend, `tsc`/`eslint`/`jest` intégralement propres. **Testé en conditions réelles de bout en bout** (comptes OWNER/MANAGER jetables, vraie base Supabase, `email_confirm` posé directement via l'API Admin Supabase pour valider la connexion sans étape de confirmation email manuelle) : création de bien → recherche du gestionnaire par email → création du mandat (`PENDING`) → visible par le gestionnaire via `GET /api/mandates` → acceptation (`ACTIVE`) → bien visible dans `GET /api/manager/portfolio` → droits opérationnels transférés (`PATCH /properties/:id` réussi pour le gestionnaire, `403` pour le propriétaire redevenu lecture seule) → révocation par le propriétaire → droits immédiatement rétablis (`PATCH` de nouveau `200` pour le propriétaire, `403` pour le gestionnaire) → tentative de ré-acceptation d'un mandat révoqué refusée (`409`). Comptes de test anonymisés (`DELETE /api/profile`) après validation, bien/mandat de test laissés en base à l'état terminal `REVOKED` (même traitement qu'un cas réel, aucune suppression physique prévue par le produit).
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé 1 problème "Important" et 4 "Mineurs", tous corrigés (Mode 1 — corrections ciblées). **Important** : `create()` lançait les créations multi-biens via `Promise.all` sur le même client `tx` à l'intérieur d'une transaction interactive Prisma — seul endroit du projet à s'écarter du pattern établi partout ailleurs (`await` séquentiel ou `createMany`, ex. `auth.service.ts`). Une transaction interactive tient une connexion unique ; des requêtes concurrentes dessus ont un comportement non garanti (risque réel sous un pooler en mode transaction comme celui de ce projet). Ni les tests unitaires (mock résolu séquentiellement) ni le test en conditions réelles initial (un seul `propertyId` dans le tableau) n'avaient exercé le vrai scénario multi-biens — corrigé en boucle séquentielle `for...of`. **Mineurs** : `@ArrayMaxSize(100)` ajouté sur `CreateMandateDto.propertyIds` (aucune borne haute auparavant) ; `propertyIds` dédupliqué avant vérification (un doublon produisait un `NotFoundException` trompeur "bien introuvable" au lieu du vrai problème) ; validation `endDate > startDate` ajoutée (aucune vérification avant, un mandat incohérent était accepté) ; oracle d'énumération de comptes sur `GET /api/managers/search` (tout `OWNER`/`MANAGER` authentifié peut vérifier si un email/téléphone correspond à un compte `MANAGER`) signalé mais **non corrigé** — comportement inhérent à la fonctionnalité demandée, cohérent avec le niveau de risque déjà accepté ailleurs dans le projet (aucun endpoint, y compris `login`, n'a de throttle plus strict que le quota global 100 req/60s). 3 tests unitaires ajoutés pour couvrir précisément le cas qui avait laissé passer le bug (multi-biens séquentiel, déduplication, `endDate` invalide) — 281 tests au total. **Retesté en conditions réelles** avec un lot de 3 biens mandatés en un seul appel (3 lignes `Mandate` distinctes, chacune avec le bon `propertyId`), `endDate` invalide refusé (`400`), doublon dans `propertyIds` traité comme un seul bien (`201`, un seul mandat créé). Comptes de test anonymisés après validation.
- [x] 32 Tableau de bord gestionnaire — 2026-08-08 (`/architect` le même jour, alignement de vocabulaire poussé) — nouveau module `src/modules/dashboard/` : `DashboardManagerService`/`DashboardManagerController` (`GET /api/dashboard/manager/{summary,revenue,alerts,owners,upcoming-payments}`, `GET /api/dashboard/manager/properties/:id/performance`). Première utilisation de requêtes d'agrégation Prisma (`groupBy`/`aggregate`) dans ce backend — jusqu'ici seuls des `_count` simples existaient (`admin.service.ts`).
      **Vocabulaire — le développeur a corrigé ma définition initiale** : « bien géré » = uniquement les biens sous `Mandate` `ACTIVE`, jamais les biens propres du gestionnaire (même s'il en possède). `/summary` et `/owners` sont donc toujours scopés mandat, sans filtre. Nouveau helper `propertyScopeWhere()` (`dashboard-scope.ts`) décline ça en 3 variantes explicites (`MANAGED`/`OWNED`/`ALL`), le cas `ALL` déléguant à `propertyVisibilityWhere()` existant plutôt que de dupliquer l'`OR`.
      **Périmètre de filtres volontairement réduit** (décision prise avec le développeur) : seulement `period` (`MONTH`/`YEAR` — semaine/trimestre/date libre explicitement retirés du périmètre) + `scope`, jamais la combinatoire complète `ownerId`/`propertyId`/statut/type sur les 6 endpoints demandée par le build-plan d'origine. `ownerId` seulement sur `/revenue` (restreint le volet « sous mandat »).
      **« Statut de paiement » simplifié** (décision prise avec le développeur après un aller-retour de clarification) : le filtre par statut a été entièrement retiré du périmètre commun — « retard » et « impayé » n'ont jamais été qu'un seul état réel (`PaymentScheduleEntry.status = OVERDUE`) dans ce système, et « en attente de confirmation » (`Payment.status = PENDING_CONFIRMATION`) reste uniquement un bucket dédié de `/alerts`, jamais un filtre transverse.
      **« Taux de recouvrement à temps »** (`/properties/:id/performance`) : % des échéances déjà échues sur les 6 derniers mois ayant atteint `PAID` sans jamais être passées par `OVERDUE` (`overdueAlertSentAt` resté `null`) — validé avec un exemple concret pendant `/architect`, et confirmé en conditions réelles (voir plus bas : un paiement réglé après relance a bien compté comme « pas à temps »).
      Accès à `/performance` vérifié via `canActOnProperty()` (propriétaire réel ou mandataire actif), réutilisé sans modification. Aucune migration nécessaire.
      11 tests unitaires ajoutés (`dashboard-manager.service.spec.ts`) — 292 tests au total. **Testé en conditions réelles de bout en bout** (propriétaire + gestionnaire + tiers non mandaté, 2 biens sous mandat, 1 bail avec paiement manuel `PAID`, 1 bail avec échéances réellement passées `OVERDUE` par le vrai cron `overdue.task.ts` qui a tourné pendant le test) : `/summary` (répartition par statut correcte avant/après l'ajout d'un locataire), `/owners`, `/alerts` (3 échéances impayées + bail expirant sous 30j détectés), `/revenue` (ventilation correcte, comparaison à la période précédente), `/performance` (a correctement compté un loyer réglé après passage en retard comme « pas à temps » — validation exacte de la définition retenue), contrôle d'accès (`403` sur un bien non mandaté, `403` pour un rôle `OWNER` sur ces endpoints réservés `MANAGER`). Comptes de test anonymisés après validation.
      **Revue `/code-review` multi-agents (3 agents en parallèle, même jour) sur les unités 31+32 combinées** a trouvé 6 signalements, tous vérifiés individuellement avant correction (2 écartés comme faux positifs, 4 corrigés) : - **Critique, corrigé** : `TenantsService.getTenantLeasesHistory()` (`tenants.service.ts`, unité 14) construisait son propre filtre de relation gestionnaire↔bien en dur (`property: { mandates: { some: { managerId: user.id } } } }`) **sans filtrer `status: 'ACTIVE'`** — seul endroit du projet oublié lors du sweep `/review` de l'unité 14 (2026-07-09). Resté invisible pendant tout le projet parce qu'aucune ligne `Mandate` réelle n'existait en base avant l'unité 31 — la clause était toujours vacuously fausse. Avec de vrais mandats désormais, un gestionnaire dont le mandat a été **refusé ou révoqué** pouvait quand même consulter l'historique complet des baux (téléphone, email, loyer, dépôt de garantie, motif de résiliation) d'un locataire sur un bien qu'il n'avait plus — ou jamais eu — le droit d'opérer. Corrigé par l'ajout de `status: 'ACTIVE'` au filtre ; test de régression ajouté (`tenants.service.spec.ts`) vérifiant explicitement qu'un mandat non-`ACTIVE` ne donne aucun accès. - **Important, corrigé** : `period.ts` mélangeait des dates construites en UTC (`Date.UTC(...)`) avec des fonctions calendaires `date-fns` sensibles au fuseau **local** du process (`startOfMonth`/`endOfMonth`/`subMonths`/etc.) — si l'hôte tourne dans un fuseau différent d'UTC, les bornes de mois/année calculées pour `/revenue` auraient été décalées d'une unité, faussant silencieusement le calcul sans qu'aucune erreur n'apparaisse. Sans impact dans ce sandbox (`Africa/Lome`, UTC+0), mais rien dans le code ne garantissait ce fuseau. Corrigé en recalculant les bornes en arithmétique `Date.UTC` pure, sans dépendre d'aucune fonction calendaire tierce. Nouveau fichier `period.spec.ts` (9 tests : bascule janvier→décembre, fin décembre, février bissextile/non-bissextile, année) — l'absence d'un tel test avait laissé passer le bug initialement. - **Mineurs, corrigés** : `'MONTH' as never` dans `dashboard-manager.service.ts` (contournement parce que `DashboardPeriodType` n'était pas importé — remplacé par un import propre) ; `dashboard-scope.ts` réimplémentait l'`OR` de `propertyVisibilityWhere()` au lieu de l'appeler pour le cas `ALL` (corrigé, délègue maintenant directement). - **Écartés après vérification (faux positifs)** : (1) le check `property.ownerId !== user.id` inline dans `MandatesService.create()` — signalé comme une violation de l'invariant `canActOnProperty()`, mais c'est une décision architecturale déjà actée et commentée dans le code : créer un mandat est un acte de propriété, jamais un droit délégable via `canActOnProperty()` (qui autoriserait à tort un gestionnaire déjà mandaté à créer un sous-mandat). (2) `MandatesService.getManagerPortfolio()` sans `select` explicite sur `Property` — vrai constat, mais identique au pattern déjà en place pour ce même modèle dans `PropertiesService.findAll()`/`getPropertyOrThrow()` ; le corriger seul créerait une incohérence plutôt que d'en résoudre une. - **Noté mais non corrigé, faible impact** : `MandatesService.create()`'s check applicatif « pas déjà de mandat `ACTIVE` » a une fenêtre de course théorique (lecture `count()` puis transaction séparée) — mais ne viole aucun invariant : la contrainte unique partielle en base reste la vraie garantie (déjà vraie et documentée symétriquement pour `accept()`). Au pire un propriétaire reçoit un `201` pour un mandat qui échouera proprement en `409` à l'acceptation. 302 tests au total après ces corrections, `tsc`/`eslint` intégralement propres.
- [x] 33 Rapports mensuels automatiques — 2026-08-08 (`/architect` le même jour, plusieurs réductions de périmètre actées avec le développeur) — nouveau module `src/modules/manager-reports/` (`ManagerReportsService`, `MonthlyReportPdfService`, `ManagerReportsController` : `GET /api/manager/owners/:ownerId/report/preview.pdf`), nouveau `src/modules/scheduling/monthly-reports.task.ts` (`@Cron('0 8 1 * *')`, nouvelle constante `CRON_MONTHLY_REPORTS`). Template email `monthly-report.template.ts` (existait déjà depuis l'unité 04, jamais branché — variables mises à jour, aucun nouveau fichier).
      **Décisions actées avec le développeur, qui réduisent le périmètre du build-plan d'origine** : (1) **rapport consolidé par propriétaire mandant**, jamais par mandat/bien individuel — un propriétaire ayant confié plusieurs biens au même gestionnaire ne reçoit qu'un seul rapport ; endpoint de prévisualisation en conséquence sur `manager/owners/:ownerId/...`, pas `manager/mandates/:id/...`. (2) **« note libre du gestionnaire » entièrement retirée** — pas de champ, pas de migration, pas d'endpoint `POST .../note`. (3) **« évolution du taux d'occupation » entièrement retirée**. Le rapport se limite donc à 3 sections : paiements reçus par bien, retards/impayés en cours (`PaymentScheduleEntry.status = OVERDUE`, réutilisé tel quel de l'unité 32), déclarations de paiement traitées (confirmées via `confirmedAt`, rejetées via `updatedAt` faute de champ dédié) — **aucune migration nécessaire**.
      **Un mandat accepté en cours de mois n'est compté pour les paiements/déclarations qu'à partir de son acceptation** — mais les retards/impayés en cours restent comptés sans cette restriction (état actuel du bien, pas une mesure de performance du mandat). `ManagerReportsService.getConsolidatedReportData()` est l'unique source de vérité du contenu, réutilisée à l'identique par le cron et par la prévisualisation, pour qu'ils ne divergent jamais.
      **Cron** : regroupe les mandats `ACTIVE` par paire (`managerId`, `ownerId`) pour dédupliquer, `try/catch` isolé par paire, `withAdvisoryLock()` obligatoire — premier cron **mensuel** du projet (les précédents sont horaires ou quotidiens).
      15 tests unitaires ajoutés (`manager-reports.service.spec.ts`, `monthly-report-pdf.service.spec.ts`, `monthly-reports.task.spec.ts`, tous nouveaux) — 317 tests au total. **Testé en conditions réelles** (propriétaire + gestionnaire mandaté + gestionnaire tiers non mandaté, 1 bien, 1 bail, 1 paiement manuel `PAID`) : prévisualisation PDF réussie (vrai PDF, en-tête `%PDF-` vérifié), `404` pour le gestionnaire non mandaté (réutilise naturellement le `NotFoundException` de « aucun mandat actif »), `403` pour un rôle `OWNER` (endpoint réservé `MANAGER`). Le cron lui-même (couvre le mois précédent) n'a pas été déclenché en conditions réelles, validé par tests unitaires uniquement. Comptes de test anonymisés après validation.
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé 2 problèmes "Important", les deux corrigés (Mode 1 — corrections ciblées) : (1) **`MonthlyReportPdfService` ne gérait aucun débordement de page** — le curseur `y` s'incrémentait sans jamais vérifier la hauteur de page ni appeler `doc.addPage()`. Invisible dans le test manuel (un seul paiement, zéro impayé) car `ReceiptPdfService` (dont ce service reprend le pattern) a un contenu de taille fixe, contrairement aux listes de longueur variable d'un rapport mensuel. Corrigé par un helper `ensureSpace()` appelé avant chaque ligne dynamique ; test dédié avec 60 échéances impayées vérifiant un vrai passage à la page suivante (comptage des objets `/Type /Page` dans le PDF brut). (2) **`MonthlyReportsTask` plafonnait à `take: 1000` sans pagination** sur `mandate.findMany({status:'ACTIVE'})` — contrairement aux autres `take: 100` du projet qui bornent les données d'un seul utilisateur, cette requête porte sur l'ensemble des mandats actifs de la plateforme ; au-delà de 1000, des paires (gestionnaire, propriétaire) auraient silencieusement arrêté de recevoir leur rapport. Corrigé par une pagination par curseur (`collectActiveMandatePairs()`, pages de 500, `orderBy: {id:'asc'}`) qui parcourt la totalité des mandats actifs quel que soit leur nombre. 4 tests unitaires ajoutés pour ces deux correctifs — 317 tests au total confirmés après correction, `tsc`/`eslint` intégralement propres. Aucune fuite de confidentialité trouvée (vérifié spécifiquement, confirmé par le test en conditions réelles avec le gestionnaire non mandaté).
      **Contre-revue des deux correctifs (`/review` puis `/recover`, même jour)** — les correctifs eux-mêmes vérifiés solides sur tous les cas limites tracés à la main (0 résultat, exactement 500 mandats, dernière page partielle). 1 problème "Mineur" trouvé et corrigé : `ensureSpace()` n'était pas appelé avant les 3 messages d'état vide (« aucun paiement »/« aucun impayé »/« aucune déclaration »), qui ne restaient protégés que par la vérification du titre de section juste au-dessus — sans impact visible avec les marges actuelles, mais fragile si `PAGE_BOTTOM` change un jour sans que cette dépendance implicite soit remarquée. `ensureSpace()` ajouté aux 3 endroits par cohérence. 317 tests toujours au total (aucun test supplémentaire nécessaire, déjà couvert par les tests existants).
- [x] 34 Profil public et avis — 2026-08-08 (`/architect` le même jour) — nouveau module `src/modules/manager-reviews/` : `ManagerReviewsService`/`ManagerReviewsController` (`POST /api/managers/:id/reviews`, `PATCH /api/managers/:id/reviews/:reviewId`), `PublicManagersController` (`GET /api/public/managers`, `GET /api/public/managers/:id`), plus extension d'`AdminModule`/`AdminController` (`PATCH /api/admin/reviews/:id/moderate`). **Dernière unité de la Phase 8** — Phase 8 (Gestionnaires immobiliers) intégralement terminée.
      **Migration** (première depuis l'unité 15) : `@@unique([ownerId, managerId])` sur `ManagerReview` — un avis par relation propriétaire↔gestionnaire, jamais par mandat individuel (aucune contrainte n'existait jusqu'ici sur ce modèle posé dès le schéma initial mais jamais utilisé par aucun code). Migration générée via `prisma migrate diff` puis appliquée par `prisma migrate deploy` (le mode interactif habituel de `migrate dev` n'est pas supporté dans cet environnement sandboxé). **Profité de l'occasion pour appliquer aussi** `20260730085655_remove_identity_verification`, restée en attente depuis l'audit du 2026-08-06 — confirmé sans risque avec le développeur avant application (colonnes déjà inutilisées par aucun code depuis le retrait de l'unité 07).
      **Décisions prises avec le développeur (`/architect`)** : (1) le droit de laisser un avis repose sur `Mandate.acceptedAt IS NOT NULL` (mandat ayant été accepté un jour, actif ou révoqué depuis) — pas seulement `status: ACTIVE`, conformément au « ayant ou ayant eu un mandat actif » du cahier des charges ; vérifié en conditions réelles avec un mandat explicitement révoqué avant la création de l'avis. (2) Avis modifiable (`PATCH`) mais jamais supprimable. (3) Modération construite dans `AdminModule` dès maintenant malgré son périmètre encore très partiel (unité 37 non terminée) — un seul endpoint, aucune dépendance sur le reste du panel admin. (4) `ManagerProfile.ratingAverage`/`ratingCount` recalculés de façon synchrone (même transaction que la mutation) à la création, la modification et la modération d'un avis — jamais un cron, le volume par gestionnaire reste modeste ; les avis `isHidden: true` sont exclus du calcul et de l'affichage public. (5) Portfolio du profil public = biens sous mandat `ACTIVE` uniquement (état actuel, jamais l'historique) — réutilise le filtre déjà en place dans `MandatesService.getManagerPortfolio()`, avec une agrégation par type de bien (`groupBy`, même pattern que l'unité 32) et un `select` restreint (jamais d'adresse/loyer de bien exposé publiquement). (6) Filtre « ville d'intervention » de l'annuaire sur `ManagerProfile.zonesOfIntervention`, jamais `User.city` (qui n'est que le lieu de résidence personnel du gestionnaire, sans logique métier). (7) Filtre « fourchette de tarifs » retiré de l'annuaire — `pricingNote` est un champ texte libre non structuré, reste affiché sur le profil détaillé mais non filtrable. (8) Noms des auteurs d'avis tronqués sur la page publique (prénom + initiale du nom), jamais le nom complet, jamais email/téléphone.
      **Aucune duplication de `canActOnProperty()`** : l'éligibilité à laisser un avis est une question différente de « qui peut agir sur ce bien » (même raisonnement déjà validé pour `MandatesService.create()` aux unités 31/32) — pas de logique d'autorisation dupliquée.
      14 tests unitaires ajoutés (`manager-reviews.service.spec.ts`, nouveau) — 331 tests au total. **Testé en conditions réelles de bout en bout** (propriétaire + gestionnaire + tiers promu `ADMIN` directement en base, 1 bien, 1 mandat accepté puis révoqué) : avis créé avec succès malgré la révocation du mandat (confirme « ayant EU un mandat »), `403` pour un tiers sans aucun mandat, `409` sur doublon d'avis, annuaire public et profil détaillé corrects (note recalculée à `4`, portfolio à `0` car le mandat n'est plus `ACTIVE`), modification de l'avis par son auteur réussie, `403` pour un tiers essayant de le modifier, modération admin réussie (avis masqué disparaît du profil public, note recalculée à `0`/`0`). Comptes de test anonymisés après validation.
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé 1 problème "Mineur", corrigé (Mode 1) : `PATCH /api/managers/:id/reviews/:reviewId` n'utilisait jamais le `:id` (managerId) du chemin — le service ne vérifiait que l'auteur de l'avis, jamais que l'avis appartenait réellement au gestionnaire indiqué dans l'URL. Aucun risque de sécurité réel (seul l'auteur peut jamais modifier son propre avis, quel que soit le `managerId` fourni), mais contredisait la sémantique REST attendue d'une ressource imbriquée. Corrigé en vérifiant `review.managerId === managerId` (sinon `404`). Aucune course sur la contrainte unique, aucune fuite de confidentialité, aucune divergence de `recomputeRating()` entre les 3 chemins d'appel — vérifié spécifiquement, rien trouvé. 1 test ajouté pour ce correctif — 331 tests toujours au total, `tsc`/`eslint` intégralement propres.

### Phase 9 — Abonnements

- [x] 35 Forfaits et quotas — 2026-08-08/09 (`/architect` le 2026-08-08) — nouveau module `src/modules/subscriptions/` : `SubscriptionsService`/`SubscriptionsController` (`GET /api/subscription/quota`, `POST /api/subscription/upgrade`, `POST /api/subscription/cancel`), `SubscriptionsModule`. `SUBSCRIPTION_TIERS`/`BETA_FREE_MONTHS` ajoutés à `src/common/constants.ts`. **Unité 36 (prélèvement mensuel automatique) explicitement reportée** avant même le `/architect` — dépend de Cashpay, déjà bloqué produit depuis 2026-07-25 (agréments T-Money/Flooz non obtenus) ; décision confirmée par question à choix (« Unité 35 seule, unité 36 reportée »).
      **Vocabulaire — renommé pour éviter une collision avec l'unité 32** : « bien géré » (unité 32) désigne un bien sous mandat `ACTIVE` pour un gestionnaire ; l'unité 35 introduit donc « bien facturable » — bien avec un locataire actif (`Lease.status='ACTIVE'`) OU une annonce active (`Listing.status='ACTIVE'`) OU au statut `RENOVATION`, jamais archivé. La mention « bail en préparation » du cahier des charges (glossaire, voir section Abonnements ci-dessous) a été retirée du périmètre — n'existe pas dans le schéma, confirmé avec le développeur.
      **Décisions prises avec le développeur (`/architect`)** : (1) tarifs/quotas consolidés : Starter (2 000 FCFA, 5 biens facturables) / Pro (5 000 FCFA, 15) / Premium (10 000 FCFA, illimité). (2) **Un seul point d'insertion pour la garde de quota** : `PropertiesService.create()` — le cycle de vie bien/bail/annonce déjà existant (auto-publication à la création, désactivation de l'annonce à la création du bail, republication à la résiliation) garantit que le compteur de biens facturables ne peut réellement augmenter qu'à la création d'un bien ; créer un bail ou repasser en `RENOVATION` ne fait qu'échanger la raison d'être facturable d'un bien déjà compté, jamais en ajouter un nouveau — les 2 autres points cités par le build-plan d'origine sont donc sans effet et volontairement ignorés. (3) `Subscription` (Starter + `betaUntil` = +3 mois) créée automatiquement à l'inscription, dans la même transaction que `User`+`OwnerProfile`/`ManagerProfile` (`AuthService.createConfirmedAccount()`, partagé par `signupOwner()`/`signupManager()`) — jamais de cas « sans abonnement » à gérer ailleurs ; restreint au typage à `OWNER`/`MANAGER`, jamais `ADMIN`. (4) `upgrade()` : uniquement vers un forfait strictement supérieur (`ForbiddenException` sinon), jamais de downgrade via cet endpoint. (5) `cancel()` : purement déclaratif pour l'instant (`status: PENDING_CANCELLATION`, `cancelAt`) — sans conséquence de facturation réelle tant que l'unité 36 n'existe pas.
      Script de rétro-remplissage `scripts/backfill-subscriptions.js` (suit le pattern non-TypeScript déjà établi par `scripts/create-admin.js`) — exécuté sur la vraie base le 2026-08-09 : 7 comptes `OWNER`/`MANAGER` sans abonnement mis à jour (Starter + bêta fraîche à partir de la date d'exécution, jamais rétroactive à la date d'inscription réelle).
      12 tests unitaires ajoutés (`subscriptions.service.spec.ts`, nouveau) — 345 tests au total. **Testé en conditions réelles de bout en bout** (compte OWNER jetable) : quota initial (`5`/`0`), création de 5 biens, quota saturé (`remaining: 0`), 6ᵉ bien rejeté (`409`, message clair), tentative d'« upgrade » vers le même forfait rejetée (`403`), upgrade vers Pro réussi (quota recalculé à `15`, `6` biens facturables comptés après un 6ᵉ bien créé), 6ᵉ bien accepté après upgrade, `cancel()` confirmé (`PENDING_CANCELLATION` + `cancelAt`). Compte de test anonymisé après validation.
      **Revue post-implémentation (`/review` puis `/recover`, même jour)** a trouvé 1 problème "Important" et 2 "Mineurs" : **Important, corrigé** — fenêtre de course (TOCTOU) entre `assertQuotaAvailable()` (hors transaction) et l'insertion du bien : deux créations concurrentes du même propriétaire juste sous le quota pouvaient toutes les deux passer la vérification puis insérer, sans aucun filet de rattrapage en base (contrairement au pattern mandats de l'unité 31, protégé par une contrainte unique `P2002`). Corrigé en déplaçant `assertQuotaAvailable()` à l'intérieur de la transaction d'insertion (`PropertiesService.create()`), précédée d'un verrou consultatif Postgres par propriétaire (`pg_advisory_xact_lock(hashtext(ownerId))`, auto-libéré au commit/rollback) — sérialise les créations concurrentes du même propriétaire sans bloquer les autres. **Retesté en conditions réelles avec une vraie course** (2 requêtes `POST /properties` simultanées à la limite du quota, via curl en arrière-plan) : exactement une `201` et une `409`, compteur final resté à `5`/`5` — jamais `6`. **Mineur, corrigé** — `upgrade()` réactivait déjà silencieusement un abonnement `PENDING_CANCELLATION` sans que ce soit une décision explicite ; comportement confirmé volontaire (un upgrade annule une résiliation en attente), rendu explicite par un commentaire plutôt que changé. **Mineur, non actionnable** — `LeaseStatus.EXPIRED` existe dans le schéma mais aucun code ne l'assigne encore (pas de cron d'expiration automatique construit) ; noté pour une future unité qui ajouterait cette transition, à republier l'annonce comme le fait déjà la résiliation manuelle. 345 tests confirmés après correction (1 test ajouté pour le verrou), `tsc`/`eslint` intégralement propres.
- [ ] 36 Prélèvement mensuel et politique de lancement — **reporté** (2026-08-08, avant `/architect`) : dépend intégralement de Cashpay, déjà bloqué produit depuis le 2026-07-25 (agréments mobile money T-Money/Flooz non obtenus, aucune date de reprise connue — voir Open Questions). Le développeur choisit de continuer sur le reste du plan en attendant.

### Phase 10 — Administration

- [ ] 37 Supervision plateforme — **partiellement construit, date exacte inconnue (avant le 2026-08-06, non documenté au moment du changement)** : `src/modules/admin/{admin.module,admin.controller,admin.service}.ts`, importé dans `app.module.ts`. Seulement `GET /api/admin/users` (liste paginée, filtres `role`/`search`, `@Roles(ADMIN)`) et `GET /api/admin/users/:id` (détail). Construit pour alimenter les pages frontend `/admin/comptes`. **Écarts avec le build-plan** : query params bruts (`@Query() role?: string`), pas de DTO `class-validator` (contrairement à l'invariant du projet) ; aucun test (`admin.service.spec.ts`/`admin.controller.spec.ts` absents) ; pas de `POST /admin/users/:id/suspend`/`reactivate`, pas de `GET /admin/transactions`, pas de `GET /admin/identity-verifications` (n'a plus de sens sans l'unité 07). 4 warnings ESLint (retours de fonction non typés). Le frontend (`lib/admin.ts`) définit aussi des appels vers `/admin/comptes`, `/admin/transactions`, `/admin/litiges`, `/admin/statistiques` qui n'existent pas côté backend — commentaire explicite déjà présent dans ce fichier frontend signalant le gap.
- [ ] 38 Tableau de bord administrateur
- [ ] 39 Gestion des litiges et signalements

### Phase 11 — Sécurité, conformité et observabilité

- [ ] 40 Journalisation, rate limiting et headers de sécurité
- [ ] 41 Sauvegardes, export RGPD et conformité

---

## Decisions Made During Build

### Stack et infrastructure

- **Backend en NestJS 10+ avec TypeScript strict**, hébergement Railway, base de données PostgreSQL via Supabase. Prisma comme ORM (et non TypeORM). Frontend Angular 20 (hors périmètre de ce dépôt, géré séparément).
- **Cron Railway horaire global** retenu plutôt qu'une file de jobs (BullMQ + Redis) ou un service externe (Trigger.dev) — le cron tourne à fréquence fixe et la base de données décide qui doit recevoir quoi. Approche simple, robuste, sans dépendance supplémentaire. Décision prise après évaluation : un rappel de loyer n'a pas besoin d'être précis à la minute, donc cette approche suffit pour la V1.
- **Cashpay (Semoa) confirmé comme agrégateur de paiement** pour T-Money et Flooz. Plan B mental gardé (PayGate Global ou CinetPay) si la documentation Cashpay reste inaccessible ou leur support trop lent à l'intégration.

### Authentification et identité

- **Supabase Auth utilisé pour toute la gestion des comptes** — pas de bcrypt côté NestJS, pas de table `password` locale. Le `SupabaseAuthGuard` valide le JWT à chaque requête authentifiée et synchronise l'`User` côté Prisma.
- **Connexion par email + mot de passe directe**, ouverture immédiate d'une session pleine sans étape supplémentaire — pas de 2FA à la connexion. L'OTP par email à 6 chiffres ne sert que pour le « mot de passe oublié » (expiration 10 minutes, usage unique, toute nouvelle demande invalide les codes précédents).
- **Confirmation d'email entièrement gérée par Supabase, zéro endpoint de vérification côté NestJS** — `generateLink({ type: 'signup' })` crée le compte Supabase ET renvoie le lien en un seul appel (pas de `admin.createUser()` séparé). Le lien est envoyé via le template WARAH `signup-confirmation` (Resend), pas l'email par défaut de Supabase, mais c'est Supabase qui héberge et valide la confirmation elle-même (`{SUPABASE_URL}/auth/v1/verify?...`) puis redirige vers `FRONTEND_URL`. Confirmé en conditions réelles : Supabase refuse lui-même le `sign-in` tant que l'email n'est pas confirmé (erreur `email_not_confirmed`), en plus du rejet `403 EMAIL_NOT_CONFIRMED` ajouté dans `SupabaseAuthGuard` (défense en profondeur).
- **⚠️ Les 6 puces suivantes (vérification CNI) décrivent une décision produit annulée le 2026-08-04** — le module identity et tout ce qu'il impliquait ont été intégralement retirés du code (voir Current Status). Conservées pour l'historique de la décision, pas pour l'état actuel : aucune vérification d'identité n'existe plus dans le produit à ce jour.
- **Vérification automatique de la CNI togolaise via Tesseract.js** (OCR). Aucune validation manuelle, aucune file d'attente humaine — décision automatique `VERIFIED` ou `REJECTED` en moins de 10 secondes, avec basculement en mode asynchrone au-delà. L'utilisateur peut re-soumettre immédiatement en cas de rejet.
- **CNI obligatoire pour propriétaire et gestionnaire, optionnelle pour locataire**. Tant que le statut n'est pas `VERIFIED`, le propriétaire/gestionnaire peut se connecter mais reste en accès restreint (lecture seule).
- **Pipeline OCR CNI entièrement asynchrone, jamais dans le request handler** (invariant #15 d'`architecture.md`, qui cite l'OCR nommément). `POST /api/identity/verify` uploade l'image, crée une ligne `IdentityVerification` en `PENDING` et répond `202 Accepted` immédiatement ; `IdentityVerificationListener` traite l'OCR en arrière-plan via un événement interne (`EventEmitter2`, `identity.verification.requested`) et met à jour le statut. `GET /api/identity/status` sert au client à suivre la transition. Pas de nouvelle infra (pas de BullMQ/Redis) — décision cohérente avec le choix déjà pris pour les crons.
- **Sérialisation par utilisateur via contrainte unique partielle en base** (`identity_verifications_user_pending_unique` — une seule ligne `PENDING` par `userId`), pas de verrou Postgres applicatif (`pg_advisory_lock`) : plus simple, pas de risque de désynchronisation avec le pool de connexions Prisma, pas de transaction longue ouverte pendant l'OCR. Une ligne `PENDING` plus vieille que 20 secondes est considérée abandonnée (ex. redémarrage du conteneur en plein traitement) et réutilisée plutôt que de bloquer l'utilisateur indéfiniment — dette technique acceptée en l'absence de file de tâches durable.
- **Marqueurs CNI togolaise sans accent** (`REPUBLIQUE TOGOLAISE`, `CARTE NATIONALE D'IDENTITE`) — confirmé sur un exemplaire réel, le texte imprimé n'a aucun accent contrairement à l'hypothèse initiale du cahier des charges. Comparaison faite sur texte normalisé (accents retirés des deux côtés) pour rester robuste si l'OCR en hallucine. Pattern du numéro de CNI : `\d{4}[-\s]\d{3}[-\s]\d{4}` (3 groupes 4-3-4, ex. `0000-000-0000`).
- **Verso de la CNI obligatoire à l'upload, mais jamais gate sur la décision** — la vraie motivation n'est pas la sécurité mais la valeur métier : le verso contient la ligne « Personne à prévenir » (nom + téléphone), utile en cas de litige/impayé. Un verso illisible laisse simplement ces champs vides ; `VERIFIED`/`REJECTED` reste décidé uniquement par le recto (marqueurs + numéro). Le checksum MRZ (ICAO 9303, via la lib `mrz`) est un signal secondaire pour le même motif — bruit OCR trop fréquent sur cette police dense pour en faire une autorité de décision.

### Rôles et droits

- **Quatre rôles fixes** : `OWNER`, `TENANT`, `MANAGER`, `ADMIN`. Le rôle `MANAGER` inclut tous les droits du rôle `OWNER` sur ses propres biens, plus la capacité d'être mandataire des biens d'autres propriétaires via le modèle `Mandate`.
- **Helper `canActOnProperty()` retenu comme autorité unique** pour décider qui peut agir sur un bien — jamais une vérification inline `if (property.ownerId === user.id)` dans un service. Cette fonction renvoie en temps réel `{ canRead, canMutate, isOwner, isMandatedManager }` selon le contexte rôle × mandat actif.
- **Transfert des droits via mandat** : quand un `Mandate` est `ACTIVE` sur un bien, le gestionnaire mandataire récupère tous les droits opérationnels (création de bail, saisie de paiement, confirmation/rejet de déclarations locataire, publication d'annonces). Le propriétaire passe en lecture seule sur ce bien et reçoit le rapport mensuel. À la révocation, le propriétaire récupère immédiatement tous les droits.

### Domaine métier — biens, locataires, baux

- **Relation locataire ↔ bien strictement 1-1** au sens d'un `Lease` `ACTIVE` à la fois pour un `TenantProfile`. La création d'un nouveau bail pour un locataire déjà actif ailleurs est rejetée — sauf si le bien précédent est passé en `VACANT`. L'historique complet du locataire (anciens baux, paiements passés) est intégralement conservé lors du changement de bien.
- **Bail V1 simplifié** : `endDate` optionnelle, les autres champs (`monthlyRent`, `monthlyCharges`, `paymentFrequency`, `startDate`, `securityDeposit`) sont obligatoires. Si pas d'`endDate`, le calendrier d'échéances est généré sur 12 mois roulants et prolongé automatiquement.
- **Le gestionnaire mandaté peut créer un bail** au même titre que le propriétaire — la décision passe systématiquement par `canActOnProperty()`.

### Paiements

- **Trois sources de paiement** strictement distinctes : `CASHPAY_API` (initié par le locataire via T-Money/Flooz, confirmé par webhook), `MANUAL_OWNER` (saisi par propriétaire/gestionnaire, direct en `PAID`), `TENANT_DECLARATION` (déclaré par le locataire, passe par `PENDING_CONFIRMATION` puis confirmé ou rejeté par propriétaire/gestionnaire).
- **Idempotence stricte du webhook Cashpay** via la contrainte unique `transactionId` côté DB — jamais une vérification applicative seule. Signature HMAC vérifiée avant tout traitement.
- **Règle d'or paiements** : un paiement avec `source = CASHPAY_API` ne peut **jamais** être re-confirmé ni rejeté manuellement. Les endpoints de confirmation manuelle rejettent toute action sur ces paiements. Le webhook est la source de vérité unique.
- **Quittances PDF générées à la volée**, jamais stockées en base ni en bucket. Toutes les données nécessaires sont déjà en Prisma. PDFKit + streaming HTTP direct. Même règle pour les rapports mensuels du gestionnaire et les factures d'abonnement.

### Notifications

- **`NotifyService.notifyUser()` comme point d'entrée unique** pour toute notification métier. Push web si `notificationConsent === ACCEPTED` et abonnement actif, sinon email — jamais les deux canaux pour le même événement.
- **Exception stricte pour les emails d'authentification** (confirmation d'inscription, OTP de réinitialisation) : appel direct à `EmailService.sendEmail()`, jamais via `NotifyService`. Ces emails doivent fonctionner même sans abonnement push.
- **Pièce jointe PDF (quittance, rapport)** force le canal email même si push est disponible — un push ne porte pas de PDF.
- **Web Push via VAPID** avec la lib `web-push`. Suppression automatique des `PushSubscription` qui retournent `410 Gone` ou `404` — jamais de retry sur subscription morte.

### Cycle de vie des comptes

- **Blocage automatique des comptes inactifs** : après 60 jours sans bien enregistré pour un propriétaire (ou sans bien propre ni mandat actif pour un gestionnaire), le compte passe à `accountStatus = SUSPENDED_INACTIVITY`. Rappels via `notifyUser()` à J-30, J-7 et J-1 avant la date butoir.
- **Déblocage automatique et immédiat** dès qu'un bien est créé ou qu'un mandat est accepté — `accountStatus` repasse à `ACTIVE`, notification de bienvenue envoyée.
- **Compte bloqué = lecture seule** : l'utilisateur peut se connecter mais tous les endpoints de mutation renvoient `403 ACCOUNT_SUSPENDED` avec un message explicite.

### Abonnements

- **Trois forfaits** (construits, unité 35) : Starter (2 000 FCFA, 5 biens facturables) / Pro (5 000 FCFA, 15 biens facturables) / Premium (10 000 FCFA, biens illimités) — `SUBSCRIPTION_TIERS` dans `src/common/constants.ts`.
- **« Bien facturable »** (nommé ainsi pour éviter la collision avec « bien géré » de l'unité 32, qui désigne autre chose — un bien sous mandat `ACTIVE`) : un bien est compté s'il a un locataire actif (`Lease.status='ACTIVE'`) OU une annonce active (`Listing.status='ACTIVE'`) OU un statut `RENOVATION`, jamais archivé. **La mention « bail en préparation » du glossaire d'origine a été retirée** — n'existe pas dans le schéma (voir unité 35). Les biens simplement enregistrés ne comptent pas.
- **Pour un gestionnaire**, le quota porte sur ses biens propres uniquement (`Property.ownerId = managerId`, un `MANAGER` pouvant posséder ses propres biens — voir Notes) — les biens sous mandat entrent dans le quota du propriétaire mandant, pas dans le sien.
- **Période bêta de 3 mois gratuits** pour tout compte `OWNER`/`MANAGER` inscrit (`Subscription.betaUntil`, créée automatiquement à l'inscription — voir unité 35). Réduction promotionnelle (`promoDiscountPercent`) posée dans le schéma mais **aucune logique ne l'applique encore** — champ prêt pour l'unité 36 (reportée).

### Stockage et données

- **~~Cinq~~ Six buckets Supabase Storage uniquement** : `property-photos`, `property-documents`, `id-documents`, `manager-documents`, `payment-proofs`, `profile-photos` (ajouté à l'étape 10 — aucun des cinq premiers ne correspondait à une photo de profil utilisateur). Aucun bucket pour les quittances, rapports ou factures (générés à la volée). **`id-documents` n'est plus utilisé par aucun flux depuis le retrait de l'unité 07 (2026-08-04)** — même statut que `manager-documents`, conservé dans le type `StorageBucket` au cas où une vérification d'identité serait reconstruite plus tard.
- **Toutes les URLs servies au client sont signées** avec expiration 15 minutes max — jamais d'URL publique permanente.
- **Compression d'images obligatoire via sharp** (max 1920px, qualité 80, format WebP) à l'upload — sauf pour les CNI dans `id-documents` qui conservent leur qualité pour l'OCR.

### Résilience et production (cf. code-standards.md section Résilience)

- **Timeouts explicites sur tous les appels sortants** (10s par défaut, 3s pour le webhook handler). Pas de retry sur les appels non idempotents (init paiement Cashpay). Retry avec backoff exponentiel borné (1s/4s/16s, 3 tentatives max) sur les appels idempotents (envoi email, push, PDF).
- **Graceful shutdown** via `app.enableShutdownHooks()` pour gérer le `SIGTERM` Railway proprement, fermeture des connexions Prisma via `OnModuleDestroy`.
- **Health checks séparés** : `/health/live` (le process tourne, sans dépendance) et `/health/ready` (Prisma + Supabase répondent).
- **Verrouillage Postgres** (`pg_try_advisory_lock`) sur les jobs cron dont la duplication est dangereuse : rapports mensuels, prélèvements abonnement, blocage d'inactivité.
- **Monitoring Sentry obligatoire en production** avec filtrage des 4xx attendues dans `beforeSend`.
- **Pagination Prisma obligatoire** sur tout `findMany`, plafond dur de 100 par page.
- **UTC partout en base** (`@db.Timestamptz`), conversion en heure de Lomé (`Africa/Lome`) uniquement à l'affichage.

### Tests et déploiement

- **Tests d'intégration obligatoires** sur 7 flows critiques : webhook Cashpay (signature + idempotence), déclaration locataire → confirmation, auth Supabase, mandats, calcul des échéances, blocage 2 mois, helper `canActOnProperty()`. Couverture cible 70 % sur les services métier, 100 % sur `canActOnProperty()` et webhook Cashpay.
- **Migrations Prisma rétrocompatibles** obligatoires — jamais `DROP COLUMN` ou `RENAME COLUMN` dans la même release qu'un changement de code. Toujours en deux temps.
- **Validation des variables d'environnement au démarrage** via `class-validator` dans `src/config/env.validation.ts` — crash immédiat si une variable manque, avec message clair.

---

## Open Questions

Questions techniques en suspens, à résoudre avant l'implémentation des étapes concernées.

- **~~La vérification CNI (unité 07) doit-elle être reconstruite ?~~** — résolu 2026-08-06 : **non, décision définitive du développeur**. Le retrait du 2026-08-04 est assumé, pas un oubli. Aucune vérification d'identité prévue en V1 ; le risque déjà assumé sur l'unité 28 (adresse publique d'un logement vacant) reste donc sans garde-fou identité supplémentaire, en connaissance de cause.
- **Le périmètre définitif de l'`AdminModule` (unité 37)** — construit de façon minimale (2 endpoints de lecture) sans passer par `/architect`, découvert lors de l'audit du 2026-08-06. À aligner explicitement sur le build-plan (suspend/reactivate/transactions) ou à documenter comme un choix de périmètre réduit assumé, comme cela a été fait pour les unités 23/29/30.

- **~~Pattern exact du numéro de CNI togolaise~~** — résolu 2026-07-04 : déduit d'un exemplaire réel (CNI personnelle du développeur, utilisée ponctuellement pour l'analyse, jamais committée — voir dossier local gitignoré `apps/backend/.local-fixtures/`). Pattern figé : `\d{4}[-\s]\d{3}[-\s]\d{4}`. Reste à valider sur un plus grand échantillon si des faux rejets apparaissent en usage réel — le texte OCR brut est conservé dans `IdentityVerification.rawText` pour ajuster les règles a posteriori.
- **~~Liste exhaustive des marqueurs Tesseract~~** — résolu 2026-07-04 : marqueurs figés sans accent (`REPUBLIQUE TOGOLAISE`, `CARTE NATIONALE D'IDENTITE`), confirmés sur l'exemplaire réel — le texte imprimé n'a aucun accent, contrairement à l'hypothèse initiale. Comparaison sur texte normalisé (accents retirés) pour rester robuste aux deux cas.
- **Documentation API Cashpay** — leur documentation publique est limitée. Demander au support Cashpay les spécifications exactes : endpoints, format des requêtes d'initialisation, format des webhooks (champs, signature HMAC, headers), codes d'erreur. **Bloquant pour les étapes 17 et 18** — de toute façon reportées sur décision explicite du développeur (2026-07-25, voir Phase 4 dans Progress), la Phase 4 a été construite sans elles (flows manuel + déclaration locataire uniquement). **Reconfirmé bloquant le 2026-08-06** : les agréments mobile money (T-Money/Flooz) ne sont pas encore obtenus côté produit, indépendamment de la doc technique — pas de date de reprise prévisible, le développeur continue sur le reste du plan (Phase 8) en attendant.
- **~~Templates email~~** — résolu 2026-07-01 : le client n'avait pas de maquettes prêtes, Claude a conçu les 11 templates avec le logo/palette WARAH fournis. Le client peut encore demander des ajustements de design plus tard.
- **~~Maquette du PDF de quittance fournie par le client~~** — non reçue, mais plus bloquant : `ReceiptPdfService` construit le 2026-07-25 avec un gabarit sobre par défaut (même traitement que les emails à l'unité 04) — à faire évoluer sans changer la signature du service dès qu'une maquette officielle arrive.
- **Maquette du PDF de rapport mensuel gestionnaire** — à recevoir avant l'étape 33. (Idem.)
- **Aucun endpoint de création de bien n'existe encore (Phase 3, étapes 12-15)** — `POST /auth/invite/tenant` dépend d'un `Property` existant (`canActOnProperty()`). Testé avec un bien seedé manuellement via script ; dès que l'étape 12 sera construite, retirer cette dépendance manuelle des futurs tests.
- **~~Nom définitif du produit~~** — résolu 2026-07-01 : la marque est **WARAH**, pas DINAWA (logo reçu). Renommage complet du dépôt effectué le même jour (voir Décision de marque ci-dessus) — reste seulement le dossier racine sur le disque et le nom du projet Supabase.
- **`warah.tg` non vérifié sur Resend** — seul `lbscallcenter.com` (autre projet) est vérifié sur ce compte Resend. `RESEND_FROM_EMAIL=noreply@warah.tg` dans `.env` échouera tant que le domaine n'est pas ajouté + vérifié sur resend.com/domains. **Bloquant pour tout envoi email réel en dehors des tests.**
- **Nom du dossier racine et projet Supabase toujours "DINAWA"** — non renommés (changement structurel risqué en session active / hors périmètre "fichiers du projet"). À faire manuellement si souhaité : renommer le dossier Windows cassera les processus en cours et l'ouverture VS Code actuelle ; renommer le projet Supabase se fait depuis son dashboard.
- **`SENTRY_DSN` fourni semble incomplet** — format reçu sans protocole ni ID de projet final (`https://<clé>@<host>` au lieu de `https://<clé>@<host>/<project_id>`). Le SDK Sentry logge `Invalid Sentry Dsn` au démarrage et désactive silencieusement la capture d'erreurs — l'app démarre normalement. À vérifier dans Sentry → Settings → Client Keys (DSN) avant la mise en prod.
- **~~Migration `20260704090000_identity_verification_pending_unique` pas encore appliquée~~** — résolu 2026-07-04 : appliquée sur la vraie base Supabase (`updatedAt` + index unique partiel confirmés en base via `_prisma_migrations`).
- **~~Étape 07 non testée en conditions réelles~~** — résolu 2026-07-04 : testée en conditions réelles (app démarrée, vraie base Supabase, vrai utilisateur de test créé, appels `POST /identity/verify` + `GET /identity/status` via curl et Postman, vraie photo de CNI togolaise). **Deux bugs critiques trouvés et corrigés** :
  1. Une image illisible/corrompue faisait planter tout le process Node — `tesseract.js` (`createWorker.js`) fait un `throw` synchrone dans son gestionnaire de message interne quand aucun `errorHandler` n'est fourni à `createWorker()`, en dehors du chemin promesse de `recognize()` que `try/catch` ne peut pas intercepter. Corrigé en passant `{ errorHandler: () => {} }` à `createWorker('fra+eng', undefined, { errorHandler })` — la promesse `recognize()` rejette alors normalement.
  2. Une vraie CNI photographiée avec le téléphone tenu autrement que dans l'axe de la carte (cas très courant, aucune métadonnée EXIF fiable) donnait un `rawText` complètement incohérent et un rejet systématique — Tesseract ne redresse aucune orientation seul. Corrigé dans `identity-verification.service.ts` : la vérification essaie désormais jusqu'à 4 rotations (0°/90°/180°/270°, via `sharp`) et retient la première qui satisfait marqueurs + pattern. Budget de timeout du listener remonté de 10s à 40s en conséquence (`identity-verification.listener.ts`) — sans risque puisque le traitement est déjà entièrement en arrière-plan (invariant #15).
     Aucun des deux bugs n'était détectable par les tests unitaires initiaux (qui mockent `tesseract.js` entièrement) — seul le test en conditions réelles les a révélés. Tests de régression ajoutés pour les deux (22 tests au total sur le module identity). N'importe quel utilisateur uploadant une photo corrompue ou simplement de travers aurait fait planter le serveur ou été rejeté à tort en production — corrigé avant tout déploiement.
- **Exploitation de la zone MRZ du verso** — repérée sur l'exemplaire réel (format ICAO `I<TG...`), plus fiable qu'une recherche de texte mais changerait le contrat de l'endpoint (upload recto+verso). Écartée pour la V1, à reconsidérer si le taux de faux rejets s'avère élevé en usage réel.

---

## Notes

Notes techniques et points de vigilance à retenir au démarrage.

- **Aucun code n'a encore été écrit.** Le repository sera initialisé à l'étape 01 avec NestJS 10+, TypeScript strict, ESLint, Prettier. Suivre les patterns définis dans `code-standards.md` dès le premier commit.
- **Cohérence du glossaire à figer** — le cahier des charges contient deux mentions divergentes des quotas Starter (5 biens dans le tableau comparatif, 2 biens dans le glossaire). Décision retenue : 5 biens Starter / 15 biens Pro / illimité Premium, valeurs consolidées dans `SUBSCRIPTION_TIERS` (cf. code-standards.md). Le glossaire du cahier des charges sera corrigé à la prochaine révision.
- **Le rôle MANAGER est en réalité un super-OWNER** — il peut posséder ses propres biens (avec tous les droits d'un OWNER dessus) ET être mandataire des biens d'autres propriétaires. Bien tester les deux casquettes dans les tests d'intégration de l'étape 31.
- **Le Togo est en UTC+0 sans DST** (`Africa/Lome`). Les expressions cron en UTC reflètent donc directement l'heure de Lomé. Toujours rester explicite pour éviter les confusions futures si la plateforme s'étend hors UEMOA.
- **Cashpay reste un risque d'intégration** — leur documentation n'est pas publique. Prévoir 2 à 3 jours de marge à l'étape 17. Si blocage prolongé, basculer sur PayGate Global ou CinetPay comme plan B.
- **Tesseract.js est CPU-bound** — l'OCR de la CNI peut prendre 5 à 10 secondes selon la qualité de l'image et la charge du serveur. L'endpoint doit basculer en mode asynchrone si dépassement (étape 07).
- **Aucune commission sur les loyers** — la promesse fondatrice de WARAH est que 100 % du loyer va au propriétaire. Le code n'a donc aucune logique de prélèvement sur les paiements de loyer. Les seuls flux financiers entrants pour la plateforme sont les abonnements mensuels (étapes 35 et 36).
- **Frontend Angular 20** géré dans un dépôt séparé — le backend NestJS n'expose qu'une API REST documentée via Swagger (`/api/docs` en dev). Aucune logique de rendu côté NestJS.
- **Mettre à jour ce fichier après chaque unité terminée** — cocher la case, ajouter la date et un résumé court des fichiers créés ou modifiés. Sans cette discipline, la reprise après une pause perd tout son contexte.
