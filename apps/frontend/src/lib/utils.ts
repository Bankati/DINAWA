// Alias attendu par la convention shadcn/ui (@/lib/utils) — le projet a déjà
// `@/lib/cn` utilisé par des dizaines de composants existants ; on garde une
// seule implémentation plutôt que deux `cn()` divergents.
export { cn } from "./cn";
