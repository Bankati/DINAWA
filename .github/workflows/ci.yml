name: CI

on:
  pull_request:
    branches: [main, staging, dev]

# Un seul run par PR — annule les runs précédents si un nouveau push arrive
concurrency:
  group: ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # ── Backend ────────────────────────────────────────────────────
  backend-ci:
    name: Backend — lint / typecheck / test / build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: apps/backend/package-lock.json

      # apps/backend est autonome (son propre package-lock.json) depuis que
      # le package.json racine a été repris par le frontend (restructuration
      # Vercel, plus de champ "workspaces" — voir
      # apps/backend/contexte/progress-tracker.md).
      - name: Install dependencies
        working-directory: apps/backend
        run: npm ci

      - name: Generate Prisma client
        working-directory: apps/backend
        run: npm run prisma:generate

      - name: Lint
        working-directory: apps/backend
        run: npm run lint

      - name: Type check
        working-directory: apps/backend
        run: npm run typecheck

      - name: Unit tests
        working-directory: apps/backend
        run: npm run test

      - name: Build
        working-directory: apps/backend
        run: npm run build

  # ── Frontend ───────────────────────────────────────────────────
  frontend-ci:
    name: Frontend — lint / typecheck / build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: apps/frontend/package-lock.json

      # apps/frontend est autonome (son propre package-lock.json), même
      # pattern que apps/backend — voir ci-dessus.
      - name: Install dependencies
        working-directory: apps/frontend
        run: npm ci

      - name: Lint
        working-directory: apps/frontend
        run: npm run lint

      - name: Type check
        working-directory: apps/frontend
        run: npm run typecheck

      - name: Build production
        working-directory: apps/frontend
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: https://placeholder.up.railway.app/api
